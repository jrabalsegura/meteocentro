# Estado del desarrollo

Última actualización: 25 de septiembre de 2026.

**Estado general:** fases 0–7 implementadas y web privada desplegada en `meteocentro.joserabalsegura.com` con Podman/Quadlet rootless. El 24-9-2026 se actualiza al commit `22998d6d3de593d307dabeb87263d0b60fdfa25b`: sesiones nuevas de un año y horas aproximadas separadas de mínima/máxima según nuestro archivo de la misma fuente, conservando los valores extremos. La búsqueda mantiene el encuadre en el primer resultado con zoom 12. Se conservan la separación de marcadores según espacio disponible, el filtro de una hora, el resumen diario, la vista inicial de Madrid, el volumen PostgreSQL, los históricos, AEMET/Meteoclimatic y la política sin copias. Los diarios reportados de Meteoclimatic siguen indicando horario desconocido; su archivo diario remoto y CSV permanecen fuera del alcance. Los registros inferiores conservan la evidencia histórica de cada fase y sus comprobaciones locales.

**Alcance vigente tras revisar costes:** primera versión con AEMET y Meteoclimatic; Wunderground aplazado como ampliación opcional. El usuario ha confirmado que no dispone de clave WU y prefiere prescindir de esa red si es cara. Se ha comprobado la tarifa pública y la elegibilidad/límite de las claves PWS; ver el documento de coste. No se ha contratado ningún servicio.

## Revisión para el flujo de Claude Code — 25 de septiembre de 2026

**Petición:** adaptar el proyecto a Claude Code y eliminar errores, ineficiencias y complejidad innecesaria, con prueba local en Docker Desktop antes de GitHub y `remote`. Rama `claude/revision-flujo-claude-code`. Sin cambios de esquema, dependencias, ingesta, cálculo de históricos ni vista inicial.

**Correcciones:** `/api/v1/stations` calculaba la frescura con 1 + N consultas por estación y origen; ahora usa una consulta por página (prueba con recuento de sentencias, que falla con el código anterior). El worker planificaba mantenimiento y trabajos de ambos proveedores cada 2 s; ahora cada 30 s, manteniendo la reclamación de trabajos cada 2 s. `get_session` reutiliza su `sessionmaker`. `ops.py` muestra la causa de un fallo (`[errno 98] Address already in use`, comando y código de salida, bloqueo ocupado) sin variables ni cadenas de conexión; prueba nueva que lo comprueba. La ficha mostraba la longitud de Madrid como «-3.7° E»; ahora «3.7° O». La gestión ya no muestra «Unexpected token <» si el proxy devuelve una página HTML de error. El Nginx del contenedor comprime JS/CSS/SVG (el bundle principal pasa de 257 kB a 95 kB); el JSON de la API no se comprime.

**Simplificación:** Compose usa `Containerfile.backend` (la misma imagen que producción, bases por digest) y los arranques `meteocentro.start migrate/worker`; se elimina `api.Dockerfile`. La base local se reinicia con Docker Desktop, como ya hacía el worker. `Makefile`, `CLAUDE.md` (importa `AGENTS.md`) y `.claude/settings.json` con permisos de solo lectura/pruebas y bloqueo de lectura de `.env`.

**Validación:** Ruff y formato con el alcance de CI; 179 pruebas de backend sobre PostgreSQL 17 temporal; `test_phase0` y `test_deploy` correctos; TypeScript/Vite correctos; Playwright con fixtures: 28 recorridos correctos y uno optativo omitido. Imagen web construida y `nginx -t` correcto; compresión comprobada con curl. Stack local levantado con `make up-worker`: la base local, que estaba en `0004_history`, migró a `0006_operations`; API sana y un ciclo AEMET real correcto con la clave local. Sin cuenta local de administración todavía.

**Peticiones añadidas tras la prueba local:** (1) al separar marcadores próximos se asignaban las posiciones por ID de estación, de modo que p. ej. Tetuán y Ciudad Universitaria podían quedar invertidas; ahora se colocan por filas de norte a sur y, en cada fila, de oeste a este (recorrido nuevo que falla con el orden anterior). (2) El listado bajo el mapa muestra «Mín. hoy» y «Máx. hoy», ordenables, en temperatura, humedad y viento, con la misma regla que la ficha: diario reportado por la fuente si es de hoy; si no, resumen horario de nuestro archivo del día civil de Madrid de la misma fuente, ampliado con su lectura actual. Sin archivo ni diario reportado se muestra «—»: la lectura actual nunca se presenta como extremo. No se mezclan redes ni canales ambiguos; viento en km/h. `/api/v1/map` añade el campo `day` con dos consultas agrupadas, independientes del número de estaciones. En la base local: 189 de 200 estaciones con extremos de temperatura hoy, 140 de ellos reportados por Meteoclimatic. Validación repetida: 180 pruebas de backend, 30 recorridos de navegador correctos y uno optativo omitido, lint y compilación correctos.

**Pendiente:** revisión del usuario en local, PR y despliegue a `remote` solo cuando se autorice.

## Sesión anual y horas aproximadas de extremos — 24 de septiembre de 2026

**Cambios solicitados:** duración por defecto y máxima de sesión de 8.760 horas (365 días), tanto en cookie como en caducidad absoluta del servidor. Ejemplos y Compose actualizados; las sesiones ya emitidas mantienen su caducidad hasta volver a entrar. La consulta de sesión no prolonga su duración y se conservan revocación, CSRF y cookies seguras. El usuario retira la petición de recuperación por correo: no se añade SMTP ni se cambia ninguna cuenta.

**Mínima y máxima:** se conservan los valores actuales (reportados por el proveedor cuando existen y del archivo en los demás casos). Cada extremo muestra su propia `minimum_at`/`maximum_at` del archivo civil de Madrid de la misma estación y origen, como **«Hora aprox.»**, incluso si el valor archivado difiere del reportado, según petición expresa. No se usa la hora del último informe, no se mezclan redes y no se infiere una hora si falta archivo o hay canales ambiguos. Cobertura parcial visible. La lluvia conserva la hora de actualización y su semántica de contador. No hay cambios de ingesta, cálculo de históricos, esquema, dependencias ni vista inicial.

**Validación local:** TypeScript/Vite, Ruff y `git diff --check` correctos. **178 pruebas de backend correctas** sobre PostgreSQL 17 temporal en UTF-8, incluidas duración de cookie/servidor, límite anual y caducidad sin renovación implícita; se refuerzan horas distintas de extremos en un día con cambio de hora. La primera ejecución no llegó a las pruebas de DB por la codificación SQL_ASCII de la base temporal; corregida a UTF-8 y repetida completa. Navegador: 26 recorridos existentes correctos y uno optativo omitido; los dos nuevos fallaron inicialmente solo por el cero del mes en el formato de fecha de Chrome. Corregida la aserción de formato, **ambos recorridos nuevos pasan** a 1.440 y 390 px: valores reportados intactos, horas independientes, mismo origen, cobertura y ausencia de histórico. Captura móvil inspeccionada sin desbordamiento. Fixtures meteorológicos sintéticos, sin consultas externas.

**Preparación remota:** inspección por SSH de solo lectura: cuatro servicios activos, worker vivo, ambas redes recientes, puerto 8089, 23 GiB libres, esquema `0006_operations`, una cuenta habilitada y 20.803 observaciones en el momento del inventario. Se actualizará exclusivamente `SESSION_HOURS` de la configuración privada y las imágenes de la aplicación; se conservarán PostgreSQL, volumen, históricos, certificados y Nginx. Existe una modificación documental previa de otra tarea en este archivo, que se preserva fuera del commit de esta entrega.

**GitHub y despliegue real:** [PR #13](https://github.com/jrabalsegura/meteocentro/pull/13), commit desplegado `22998d6d3de593d307dabeb87263d0b60fdfa25b`. [CI del commit](https://github.com/jrabalsegura/meteocentro/actions/runs/36042041349) correcta en backend, interfaz y ensayo de contenedores/migraciones/persistencia; también correctas las seis comprobaciones combinadas de push/PR antes de aplicar. Suite de navegador en CI: 28 recorridos correctos y uno optativo omitido. Imágenes construidas desde el SHA exacto en `remote`; IDs Podman normalizados y verificados. Comparación previa de Quadlet y Nginx correcta: solo cambian imágenes API/worker/web. `SESSION_HOURS=8760` aplicado al archivo privado conservando sus permisos 0600 y todas las demás variables. Despliegue mediante `ops.py` correcto, sin nuevas migraciones.

**Verificación posterior:** HTTP/HTTPS, rutas SPA y protección de API/CSV/gestión correctos. Configuración efectiva de la API en producción: 8.760 horas. Cuatro unidades activas; DB/API/web saludables y worker vivo, sin trabajos atrasados y ambas redes recientes. Antes/después: **21.066 observaciones, 204 estaciones, cero exclusiones, una cuenta y esquema `0006_operations`**, con primer dato del 22-9-2026 19:00 UTC. Huellas de todos los vhosts, archivos privados ajenos a sesión y montaje PostgreSQL idénticos. Se conservan puerto 8089, certificados y otros servicios. Desde el Mac, HTTPS entrega JS/CSS idénticos byte a byte al build probado (`index-DvMh8_UP.js`).

**Ejemplos reales de la proyección desplegada, sin consultas adicionales a proveedores:** Fresno de Cantiespino (AEMET), archivo de 8,9/35,5 °C con horas 08:00/15:00 de Madrid; Majadahonda–Entreálamos (Meteoclimatic), valores reportados 16,1/35,5 °C y archivo 16,1/35,4 °C, con horas aproximadas 07:43/17:43 y cobertura 93 %. Esta diferencia confirma el comportamiento solicitado: se conserva 35,5 °C y se aproxima su hora con el máximo archivado de esa fuente. Son ejemplos puntuales, no valores garantizados para consultas posteriores.

**Límites y siguiente paso:** recargar la web y volver a entrar una vez para obtener la cookie anual; las sesiones anteriores mantienen su caducidad. Horas aproximadas, no exactitud entre muestras ni reconstrucción de huecos. No se ha hecho un login de navegador en producción ni un reinicio completo del host; la cookie/caducidad y la interfaz están verificadas en pruebas, y se han comprobado la configuración efectiva, la proyección real y la entrega HTTPS. Sin recuperación por correo, por decisión del usuario.

## Encuadre automático de búsquedas — 23 de septiembre de 2026

**Cambio local solicitado:** la búsqueda por nombre, municipio o ID centra el mapa en la primera coincidencia del orden actual del listado, con transición de 450 ms y zoom moderado 12. Espera la respuesta asociada a los filtros actuales, sin abrir la ficha ni quitar el foco del buscador. Si se busca desde la tabla, aplica el encuadre al volver al mapa. Cada búsqueda se encuadra una sola vez: refrescos, cambios de variable, ordenación y borrado del texto conservan la posición. Se mantiene Madrid como vista inicial; una URL con `view` válido prevalece también si contiene búsqueda. Sin coincidencias o sin coordenadas en la primera, no se mueve el mapa.

**Validación ejecutada:** `npm run build` correcto y suite Playwright existente con **26 recorridos correctos y uno optativo de administración real omitido**. Comprobación adicional de navegador a 1440 y 390 px con fixtures: primera coincidencia según el listado (distinta del orden de respuesta), nombre, ID, municipio, foco, refresco, variable, borrado, búsqueda vacía, coordenadas ausentes, URL y búsqueda desde tabla. Verificados además la vista inicial de Madrid, el descarte de una respuesta atrasada frente a una búsqueda posterior, error/reintento y conservación del filtro de antigüedad. Capturas inspeccionadas en ambas anchuras: marcador numérico centrado y legible, sin desbordamiento. Las consultas por ID/municipio se simulan en este recorrido; su contrato de backend no cambia. API y teselas sintéticas, sin consultas a proveedores. `git diff --check` correcto.

**Preparación de publicación y despliegue:** el usuario solicita GitHub y despliegue. Inspección por SSH: cuatro unidades activas, DB/API/web saludables, Podman rootless 4.9.3, puerto 8089 y 23 GiB libres. Volumen y esquema existentes conservados: 101 estaciones, 3.209 observaciones desde el 22-9-2026 19:00 UTC, cero exclusiones y `0006_operations`. Worker vivo sin trabajos atrasados, Meteoclimatic reciente y aviso previo de AEMET sin nuevos datos desde las 08:00 UTC. Actualización de interfaz sin nuevas migraciones; conservar configuración privada, Nginx, certificados y volumen.

**GitHub y CI:** [PR #12](https://github.com/jrabalsegura/meteocentro/pull/12), commit desplegado `31d4bbfe983bd3243408d7575e77a5085ae8beaa`. [CI del commit desplegado](https://github.com/jrabalsegura/meteocentro/actions/runs/35884459143) correcta en backend, frontend y ensayo de contenedores/persistencia; los seis checks combinados de push/PR pasaron antes de aplicar. Imágenes construidas desde el SHA exacto en `remote`, con Node 24.21.0 e IDs Podman normalizados y verificados. La comparación de Quadlet confirma que solo cambian las imágenes de API/worker/web.

**Despliegue real y verificación:** aplicación con `ops.py` y smoke HTTP/HTTPS correctos, incluidas rutas SPA y protección de API/CSV/gestión. Cuatro unidades activas, DB/API/web saludables y worker vivo sin trabajos atrasados. Antes/después: **3.249 observaciones**, primera del 22-9-2026 19:00 UTC y última del 23-9-2026 15:38 UTC, **101 estaciones y cero exclusiones**. Montaje del volumen y esquema `0006_operations` idénticos; huellas de todos los vhosts y de los archivos de configuración privada sin cambios. Se conserva el puerto 8089 y no se modifican certificados ni otros servicios. HTTPS 200 desde el Mac; bundles `index-CQz0xF9E.js` y `WeatherMap-l2_O-_Au.js` idénticos byte a byte a los compilados y probados localmente. Continúa el aviso previo de AEMET sin datos nuevos; Meteoclimatic reciente. No se fuerzan consultas a proveedores.

**Límites:** se conservan exclusiones, agrupaciones y el filtro de una hora; una coincidencia antigua permanece oculta hasta desmarcarlo y una lectura ausente no se fabrica. Sin cambios de backend, esquema ni dependencias. El sandbox impidió inicialmente abrir el servidor local; la suite se ejecutó después con el permiso correspondiente. Se mantienen los avisos conocidos de tamaño de bundles y deprecación de Node local.

**Siguiente paso:** recargar la web para probar la búsqueda. Se ha verificado la interacción con fixtures y la entrega real del mismo código por HTTPS; no se ha realizado un recorrido autenticado de navegador en producción tras esta actualización.

## Separación de marcadores según espacio disponible — 23 de septiembre de 2026

**Diagnóstico:** el grupo de Rivas corresponde a Rivas–Bellavista (`ESMAD2800000028522A`) y Rivas Urbanizaciones (`ESMAD2800000028522B`). Las fichas públicas de [Bellavista](https://www2.meteoclimatic.net/perfil/ESMAD2800000028522A) y [Urbanizaciones](https://www.meteoclimatic.net/perfil/ESMAD2800000028522B) publican las mismas coordenadas a minutos, 40°21′ N, 3°31′ O, pero distintos observadores, equipos y altitudes (622/651 m). Esto respalda tratarlas como estaciones distintas, sin inferir sus posiciones exactas. La interfaz en uso confirma ambas identidades y lecturas diferentes. La regla de 1 km es un criterio ampliado de revisión entre redes, no una distancia mínima entre estaciones; las identidades habilitadas de una misma red no se descartan por cercanía.

**Cambio solicitado y preparado localmente:** disposición automática de valores individuales cuando hay espacio alrededor, sin exigir un zoom concreto. Se reservan los demás símbolos antes de separar un grupo, se prueban filas/columnas y se limita el desplazamiento visual a 96 px; una línea señala cada ubicación original. Con densidad elevada se conserva el grupo. Posiciones e identidades permanecen estables al seleccionar o refrescar; se mantienen la vista inicial de Madrid y el filtro de una hora. Los grupos de coordenadas idénticas ofrecen directamente una lista con nombre, valor, origen, ID y hora; también se conserva esta salida en zoom alto. Corregida la propagación del clic que abría la lista y podía cerrarla inmediatamente al llegar al mapa. Se retiran las listas al actualizar su población o variable para evitar contenido obsoleto.

**Validación local ejecutada:** compilación TypeScript/Vite correcta y suite Playwright completa con **26 recorridos correctos y uno optativo de administración real omitido**. Seis casos nuevos cubren coordenadas idénticas a zoom 10/10,69/18, coordenadas cercanas, espacio libre en horizontal y vertical, densidad que requiere ampliar, apertura por teclado de ambas estaciones, lectura de grupos numerosos, cambio de variable y retirada de estaciones tras refresco. Se conserva el foco de un marcador al redibujar y la lista no se cierra por una renderización ajena a sus datos. Capturas inspeccionadas a 1440 y 390 px: valores separados y líneas en su ubicación, sin desbordamiento. El escenario de 2.000 estaciones sintéticas mantiene **cero solapamientos**. Pruebas con API/teselas simuladas, sin HTTP meteorológico externo; no se ha ejecutado una integración de datos real nueva. `git diff --check` correcto. Sin cambios de dependencias; permanecen los avisos conocidos de tamaño de bundles y deprecación del Node local.

**Preparación de publicación y despliegue:** el usuario solicita GitHub y `ssh remote`. Inspección previa: cuatro unidades activas, DB/API/web saludables, Podman rootless 4.9.3, puerto 8089 y 23 GiB libres. Worker vivo sin tareas atrasadas; Meteoclimatic reciente y AEMET con el aviso previo de respuestas sin datos nuevos desde las 08:00 UTC. Cambio de interfaz sin modificaciones de backend, esquema, radio de duplicados, coordenadas, exclusiones ni series.

**GitHub y CI:** [PR #11](https://github.com/jrabalsegura/meteocentro/pull/11), commit desplegado `34acaf99d9f126cc12c3149e2d68c0db40ce05df`. [CI del commit desplegado](https://github.com/jrabalsegura/meteocentro/actions/runs/35881617989) correcta en backend, frontend y ensayo de contenedores/persistencia; los seis checks combinados de push/PR pasaron antes de aplicar. La primera ejecución detectó una carrera en las pruebas nuevas al medir elementos durante el redibujado de MapLibre: corregida con lectura conjunta y espera de geometría, conservando las aserciones de separación y límites. La navegación por teclado espera también la restauración del foco al cerrar una ficha. Repetidos los seis casos nuevos localmente, correctos; CI completa con 26 recorridos de navegador correctos y uno optativo omitido.

**Despliegue real y comprobación:** imágenes construidas desde el SHA exacto en `remote`, con IDs Podman normalizados y verificados. Comparados los Quadlet: solo cambian las imágenes API/worker/web. `ops.py apply` y smoke HTTP/HTTPS correctos, incluidas rutas SPA y protección de API/CSV/gestión; sin nuevas migraciones. Cuatro unidades activas, DB/API/web saludables y worker vivo sin trabajos atrasados. Volumen `meteocentro-db-data`, esquema `0006_operations`, 101 estaciones, cero exclusiones y primer dato del 22-9-2026 19:00 UTC conservados. El archivo pasa de 3.129 a 3.169 observaciones durante la ingesta normal, con último dato del 23-9-2026 15:08 UTC. Huella de Nginx idéntica; puerto 8089, configuración privada y certificados conservados. HTTPS 200 desde el Mac y bundles `index--1A0tjlA.js` y `WeatherMap-DGRXtHeo.js` idénticos byte a byte a los compilados y probados localmente. Continúa el aviso previo de AEMET; Meteoclimatic reciente. No se fuerzan consultas a proveedores ni se cambian otros servicios.

**Límites y siguiente paso:** validación visual de escritorio/móvil con fixtures y verificación real de entrega HTTPS, servicios y persistencia; no se ha realizado un recorrido autenticado de navegador en producción tras esta actualización. Recargar la web para usar la nueva disposición y continuar la ingesta ordinaria.

## Filtro de estaciones sin actualizar — 23 de septiembre de 2026

**Solicitud y cambio:** casilla marcada por defecto para ocultar del mapa las estaciones cuyo dato de la variable seleccionada supera 3.600 s; exactamente una hora sigue visible. También se ocultan las que no tienen lectura utilizable. La tabla conserva su población y señala las filas antiguas en ámbar con «Más de 1 h sin actualizar» y, cuando procede, «Oculta en el mapa». Desmarcar conserva todos los marcadores y persiste en la URL (`hide_old=0`); los antiguos muestran borde discontinuo. El refresco de 60 s evalúa de nuevo la edad de observación, nunca la recogida. Contadores de mapa/listado separados y mensaje recuperable si todas quedan ocultas. Vista inicial de Madrid, filtros comunes, históricos y umbrales operativos por proveedor conservados.

**Validación local:** compilación TypeScript/Vite correcta; suite Playwright completa con **20 recorridos correctos y uno optativo de administración real omitido**. Tras reforzar la exclusión de horas futuras y ajustar el filtro móvil, repetidos los **12 recorridos de mapa, todos correctos**. Casos de 3.600/3.601 s, cero, ausencia, hora futura, AEMET todavía reciente según su umbral, Meteoclimatic entre 45–60 min, recarga, cambio de variable, tabla persistente, desaparición y recuperación automática. Captura a 390 px inspeccionada, sin desbordamiento. Fixtures sintéticos sin llamadas a proveedores; no cambia backend, esquema ni dependencias. Node local 26.3.0; CI y la imagen conservan Node 24.21.0. Se mantienen los avisos conocidos de bundles grandes y deprecación del runtime local.

**Preparación remota:** inspección SSH autorizada: cuatro unidades Meteocentro activas, DB/API/web saludables, rootless Podman 4.9.3, puerto 8089, volumen `meteocentro-db-data` y 23 GiB libres. Antes del cambio, el worker está vivo y sin trabajos atrasados; Meteoclimatic tiene datos recientes, mientras AEMET responde sin observaciones nuevas desde las 08:00 UTC. Es un estado previo del proveedor, no un fallo de esta interfaz. Se conserva el vhost HTTPS y la configuración privada existentes.

**Despliegue real:** commit `0b8ff7b05bc5b352a0cb7e40aa2365189b823777`, publicado en [PR #10](https://github.com/jrabalsegura/meteocentro/pull/10). [CI del código desplegado](https://github.com/jrabalsegura/meteocentro/actions/runs/35874180148) correcto en backend, frontend y ensayo de contenedores/persistencia; también correctos los seis checks combinados de push/PR antes de aplicar. Construcción exacta en `remote`, con normalización verificada del prefijo `sha256:` de los IDs de Podman 4.9.3. Comparados los Quadlet: solo cambian las imágenes de API/worker/web. Aplicación correcta con `ops.py`, sin nuevas migraciones.

**Verificación posterior:** smoke HTTP local y HTTPS satisfactorio, incluidas rutas SPA y protección de API/CSV/gestión. Cuatro unidades activas; PostgreSQL, API y web saludables. Antes/después: **3.010 observaciones**, primera del 22-9-2026 19:00 UTC y última del 23-9-2026 14:13 UTC, **101 estaciones y 0 exclusiones**; volumen `meteocentro-db-data` y esquema `0006_operations` conservados. Huella del vhost Nginx idéntica. Desde el Mac, HTTPS 200 y bundle `index-D2BbL64X.js` idéntico byte a byte al compilado y probado localmente. El navegador muestra el acceso privado sin avisos TLS; no se prueba un recorrido autenticado de producción porque no había sesión iniciada. Worker vivo y sin tareas vencidas; Meteoclimatic reciente y AEMET continúa con el aviso previo de respuestas sin datos nuevos. No se fuerzan consultas a proveedores.

**Siguiente paso:** uso normal del filtro y seguimiento ordinario de la ingesta; el aviso previo de AEMET depende de recibir observaciones nuevas.

## Mejora de mapa y resumen inmediato — 23 de septiembre de 2026

**Solicitud:** retirar la escala y el texto superpuestos al mapa, mostrar mínima/máxima/precipitación al abrir una estación y mantener el listado sincronizado con los filtros. El usuario confirma que el encuadre geográfico no debe recortar la tabla.

**Cambios:** eliminadas las dos superposiciones y sus estilos; se conserva la leyenda fuera del mapa y la atribución IGN. La ficha compacta y completa muestran el resumen diario sin desplegables, con humedad/viento en el resumen. Los diarios reportados conservan fuente, fecha y advertencia sobre reinicio desconocido; se rechazan fechas anteriores o futuras al resumen. AEMET usa las observaciones ya almacenadas desde medianoche de Madrid hasta la consulta, con cobertura y huecos explícitos. Se reutiliza la agregación de históricos por origen y canal, sin sumar contadores ni intervalos solapados o repartir intervalos que crucen medianoche. Nulo y cero siguen siendo distintos. El mapa y la tabla usan una sola respuesta asociada a sus filtros; se descartan respuestas canceladas y poblaciones de filtros anteriores. Se mantiene la vista inicial de Madrid.

**Validación local ejecutada:** compilación TypeScript/Vite correcta; 49 pruebas de mapa/históricos en PostgreSQL 17.11 aislado correctas; 18 recorridos Playwright correctos (uno adicional de administración con API real omitido por su configuración explícita). Casos nuevos: cero/ausencia, cobertura parcial, lluvia solapada, límite diario, exclusión, origen, diario de ayer y todos los filtros con paginación/recarga. Inspección de captura a 390 px: las tres tarjetas diarias visibles al abrir, sin desbordamiento. Ruff y `git diff --check` correctos. Datos de prueba sintéticos, sin consultas a proveedores.

**Despliegue real:** commit `ed67c0986b6b84c3b42f5e4755e7c9ed1b7eec10`, publicado en [PR #9](https://github.com/jrabalsegura/meteocentro/pull/9), construido desde GitHub en `remote` con Node 24.21.0 y aplicado mediante `ops.py`. [CI del código desplegado](https://github.com/jrabalsegura/meteocentro/actions/runs/35836225056) correcto: 177 pytest + 9 unittest, 18 recorridos de navegador y ensayo aislado de contenedores/migración/persistencia. Comparación Quadlet: solo cambian imágenes API/worker/web; conserva PostgreSQL, modo rootless, puerto 8089, volumen `meteocentro-db-data`, configuración privada, Nginx y certificados. El manifiesto normaliza los IDs hexadecimales de Podman 4.9.3 añadiendo `sha256:`, verificando antes que corresponden a las mismas imágenes; el script de construcción no se ha modificado. No hay migraciones nuevas; continúa `0006_operations`.

**Verificación posterior:** smoke HTTP local y HTTPS correcto, incluidas rutas SPA y protección de API/CSV/gestión. El navegador carga por HTTPS el bundle `index-G_gFAl0T.js` y el acceso privado; no había sesión iniciada para un recorrido autenticado de producción. Las cuatro unidades están activas; DB/API/web saludables. Antes/después: 1.835 observaciones, primera del 22-9-2026 19:00 UTC, 101 estaciones y 0 exclusiones; volumen y esquema conservados. Lectura directa de la proyección actual en la API desplegada: Madrid/Barajas devuelve mínima 15 °C, máxima 22 °C y lluvia 0 mm, con coberturas parciales 96,4 %/86,7 % (37 ms); Madrid–Vallecas devuelve diarios reportados 18,7/27,4 °C y 0 mm con su base temporal desconocida (22 ms). Son muestras reales de la comprobación, no valores garantizados para posteriores consultas. `operations`: estado correcto, worker vivo, ninguna tarea atrasada y ambas redes recientes. No se lanzaron importaciones ni consultas extra a proveedores.

**Limitaciones:** los extremos calculados representan el archivo disponible del día y la lluvia parcial solo sus intervalos válidos; no equivalen a un día completo cuando falta cobertura. Meteoclimatic sigue con día del proveedor de reinicio desconocido. Esta revisión no cambia proveedores, política de acceso ni periodicidad de recogida.

**Siguiente paso:** seguimiento ordinario de la ingesta. El reinicio completo del host y una nueva entrada autenticada en producción no se han ensayado en esta revisión.

## Seguimiento de fases

| Fase | Estado | Evidencia pendiente para cerrar |
| --- | --- | --- |
| 0 | Investigación y accesos documentados; Meteoclimatic local resuelto en fase 3 | Revisar productos publicados/archivo diario antes de esos usos; ampliar piloto histórico cuando corresponda |
| 1 | Implementación y pruebas locales completadas con Docker Compose y PostgreSQL 17.11; comprobación Podman pendiente | Ejecutar el mismo Compose con Podman en un entorno que lo tenga instalado |
| 2 | Implementada y validada con PostgreSQL real y piloto AEMET de tres ciclos | Disponibilidad sostenida y recepción de próximas publicaciones no cubiertas por el piloto limitado |
| 3 | Completada para uso local: piloto real de catálogo e ingesta Meteoclimatic; criterios de duplicados y provincia revisados el 21-9-2026 | Dos posibles duplicados y tres ubicaciones fuera del ámbito en el catálogo local actual; seguimiento sostenido al desplegar. Horario diario desconocido explícito. WU aplazado |
| 4 | Implementada y validada localmente: mapa, tabla, fichas, filtros persistentes y consulta por lote | Medir en el entorno de despliegue; la medición local no garantiza latencia de Internet ni móviles reales |
| 5 | Implementada y validada localmente con PostgreSQL, gráficos y piloto AEMET de 30 días | Diario remoto Meteoclimatic/CSV pendientes del producto autorizado; medir archivo multimétrica y aplicar migración con mantenimiento al desplegar |
| 6 | Implementada y validada con PostgreSQL y recorrido completo de navegador/API | Aplicar migración y crear administrador al actualizar la instalación; reinicio de contenedores con Podman/Quadlet en fase 7 |
| 7 | Implementada y desplegada con Podman/Quadlet rootless; actualización HTTPS y persistencia verificadas el 23-9-2026 | Ensayo de reinicio completo del host en ventana autorizada |

## Validación completa en Docker tras retirar copias — 23 de septiembre de 2026

**Petición:** el usuario ha arrancado Docker Desktop y solicita probar localmente antes de desplegar. Construidas las imágenes ARM64 desde los Containerfiles actuales, con dependencias y bases bloqueadas, reutilizando caché. Manifiesto `runtime/no-backups-docker-images.json`; son imágenes de ensayo (`dirty=true`), no una versión instalable de producción.

**Ensayo OCI correcto en 63,5 s:** cuatro contenedores nuevos, red y volumen sintéticos propios. Migración real 0005→0006→0005→0006 y Alembic `check`; huellas de datos/exclusiones conservadas. HTTP, rutas SPA y API/CSV/gestión privados correctos; login real y estación excluida ausente. API recreada de `172.21.0.4` a `172.21.0.6`, con recuperación DNS/HTTP. Detección de worker detenido mediante latido envejecido, reinicio real de PostgreSQL y recuperación del worker; huellas de catálogo, observaciones, exclusiones y auditoría conservadas. `nginx -t` del contenedor correcto. Sin dumps, tareas de copia ni restauración de base.

**Pruebas:** **184 correctas** de backend/despliegue sobre una base PostgreSQL 17.11 separada dentro del ensayo, en **17,98 s**, con los dos avisos de deprecación conocidos. **15 recorridos Playwright correctos** en Chrome, en **51,3 s**, con API/cartografía sintéticas; el recorrido optativo de administración integrada queda omitido al no configurar `E2E_REAL_ADMIN`. Playwright se ejecutó con Node 26.3.0 del Mac; la imagen web conserva Node 24.21.0 para su compilación. Generación real de las seis unidades Quadlet con Podman 4.9.3 en contenedores sin red, tanto rootless como rootful. Ruff de scripts y `git diff --check` correctos.

**Evidencias y limpieza:** informe `runtime/no-backups-docker-rehearsal/result.json`; logs de construcción, ensayo, navegador y generador en `runtime/no-backups-validation/`. Retirados todos los contenedores y la red `meteocentro-phase7-523d3c03b3`; conservado únicamente su volumen sintético `meteocentro-phase7-523d3c03b3-data`, conforme al procedimiento. Los contenedores previos siguen arrancados; no se ha modificado la base ni la configuración en uso. Los proveedores están desactivados exclusivamente en el ensayo.

**Límites y siguiente paso:** validación local ARM64 en Docker, sin SSH, despliegue, publicación de imágenes, TLS real ni ejecución de systemd en el servidor. Antes de instalar, guardar la revisión en un commit limpio accesible desde `remote`, construir las imágenes de su arquitectura y seguir [OPERACION.md](OPERACION.md). La generación Quadlet no acredita por sí sola el cierre de SSH ni un reinicio del host.

## Retirada de copias de seguridad — 23 de septiembre de 2026

**Petición y cambios:** eliminar las copias del proyecto para su uso como web personal y rehacer las instrucciones para `ssh remote`, sin ejecutar el despliegue. Retirados los comandos de copia/restauración, la copia previa a actualizar, la reserva de disco específica, la retención y las unidades service/timer. `apply` conserva el bloqueo de operaciones, la parada de escritores, la migración única, la comprobación HTTP y la protección del volumen. El ensayo y la CI verifican migraciones/persistencia mediante conteos y huellas, sin dumps ni volumen adicional de restauración.

**Documentación:** actualizados alcance, decisiones, fases 6–7, administración y [OPERACION.md](OPERACION.md). La guía explica primera instalación y actualización sin copias, arquitectura del servidor, configuración privada, cuenta administrativa y vhost HTTPS. El despliegue ordinario crea una base nueva; no traslada ni borra la base local. Conservar históricos locales en el servidor requiere concretar el traslado antes de instalar.

**Validación ejecutada:** cuatro pruebas de despliegue correctas (configuración/secretos, imágenes y rutas, actualización correcta, fallo de migración) y cuatro de operación sobre PostgreSQL real: **8 pruebas correctas**, con dos avisos de deprecación ya conocidos. PostgreSQL 17.11 temporal, sin escucha TCP ni proveedores: migración 0005→0006→0005→0006 con observación/exclusión/auditoría/cuenta sintéticas, comparación de huellas y Alembic `check` correctos; reinicio real de esa base temporal con las mismas huellas. Se detuvo y retiró únicamente ese entorno temporal. El primer intento quedó limitado por la memoria compartida del sandbox; la ejecución autorizada fuera de él completó las pruebas. Ruff, formato, ayuda CLI y `git diff --check` correctos. Evidencias locales ignoradas: `runtime/no-backups-validation/report.json`, `pytest.log` y `postgres-and-migrations.log`. Las pruebas de `apply` simulan las órdenes del host; no acreditan ejecución real de systemd/Podman.

**Límites de aquella comprobación:** Docker no estaba disponible (socket local ausente); el ensayo OCI se completó después, cuando el usuario lo arrancó, como se registra arriba. No se ejecutó SSH, publicación de imágenes, instalación systemd o cambios en el servidor. Antes de desplegar, guardar esta revisión en un commit accesible desde `remote`, comprobar el host y elegir dominio/modo/puerto. Sin copias, no hay recuperación del contenido si se pierde el volumen PostgreSQL.

## Evidencia histórica anterior a la retirada

Los registros siguientes describen lo que se ejecutó el 21-9-2026. Las referencias históricas a copias, restauración de base, retención y objetivos de recuperación están sustituidas por la decisión del 23-9-2026 y no son requisitos ni capacidades vigentes. Se conservan como trazabilidad de las pruebas anteriores.

## Corrección del ensayo de contenedores en CI — 21 de septiembre de 2026

**Fallo localizado:** en el commit `c2ddd55` de la PR #7, backend y frontend pasaron tanto en `push` como en `pull_request`. Ambos checks `containers` fallaban al reservar la IP antigua de la API: el Docker de Ubuntu exigía una subred configurada explícitamente para usar `--ip`. Las imágenes AMD64 y la generación Quadlet ya habían terminado correctamente; el fallo interrumpía el ensayo antes de su reinicio/restauración. No se trataba de los dos casos temporales del Mac ni del lint ya corregido.

**Cambio:** el motor elige primero una subred libre para la red aleatoria del ensayo. Antes de conectar contenedores, se recrea exclusivamente esa red vacía con la misma subred explícita. Se conserva la reserva de IP y la aserción de que la API cambia de dirección; el informe registra ambas IP y la subred. Los contenedores temporales se registran para su limpieza antes del arranque, cubriendo un fallo durante `run`. CI muestra la salida sin búfer y conserva los manifiestos/informes no secretos incluso cuando falla una comprobación.

**Pruebas ejecutadas:** ensayo OCI completo en Docker local **correcto en 44,8 s**; subred `172.21.0.0/16`, API `172.21.0.4` → `172.21.0.6`, recuperación DNS/HTTP, migración/downgrade, reinicio del PostgreSQL temporal y restauración aislada con huellas coincidentes. Evidencias en `runtime/phase7-explicit-subnet-check/result.json` y su log. Cuatro pruebas de seguridad de despliegue correctas; Ruff, formato y `git diff --check` correctos. No se repite aquí la suite de aplicación, cuyo código no cambia. Los resultados de Linux/AMD64 del commit corregido se consultan en los checks de la [PR #7](https://github.com/jrabalsegura/meteocentro/pull/7).

**Límites:** la validación de CI sigue siendo un ensayo sintético, sin proveedores reales ni despliegue. Los contenedores/red del ensayo local se retiraron; se conservaron sus volúmenes sintéticos, la vista previa de `8088` y todos los servicios anteriores. No se modificó la configuración de AEMET/Meteoclimatic, no se usó SSH ni se validó un reinicio del servidor.

## Entrega de fase 7 en GitHub — 21 de septiembre de 2026

El usuario da por revisada la entrega local y solicita la pull request. Abierta la [PR #7 — Fase 7: Podman, operación y despliegue reproducible](https://github.com/jrabalsegura/meteocentro/pull/7), desde `codex/fase-7-podman-operacion` contra `main`, que ya incluye la fase 6 mediante la PR #6. Incluye la infraestructura, la guía paso a paso y las correcciones de las dos pruebas intermitentes descritas debajo. La validación local se mantiene en 184 pruebas correctas y ensayo de restauración aislada ejecutado. La primera ejecución de GitHub Actions detectó alias redundantes y formato de las importaciones de fixtures de fase 7; se corrigen aplicando los mismos comandos de Ruff que usa CI. El resultado actualizado de GitHub Actions se consulta en la PR. El push ejecuta comprobaciones y construcción, sin publicación GHCR ni despliegue automático. Continúan pendientes la instalación y la validación del servidor real.

## Corrección de validación en Mac y vista previa de fase 7 — 21 de septiembre de 2026

**Incidencia comunicada:** una repetición del ensayo terminó con 182 pruebas correctas y dos fallos intermitentes, pese a la ejecución anterior correcta. `test_bounded_retry_backoff_then_normal_cadence` comparaba una fecha de PostgreSQL con `datetime.now()` del Mac; una diferencia de milisegundos producía `899.998613 < 900`. La prueba de reanudación histórica también programaba un trabajo con la hora del Mac: podía quedar unos milisegundos en el futuro para PostgreSQL y `claim()` devolver `None`. Los fallos estaban en la preparación/comprobación temporal de esas pruebas; la cola real ya usa `clock_timestamp()` de PostgreSQL.

**Corrección:** ambas pruebas usan ahora `db_now()` para la cadencia, expiración y reencolado. El límite de reintentos se comprueba entre dos lecturas del reloj PostgreSQL, incluyendo únicamente el jitter permitido, sin depender de la duración del comando ni añadir esperas. Se verifica explícitamente que los trabajos repetidos se adquieren antes de ejecutarlos. No se han cambiado los intervalos de ingestión ni los proveedores.

**Validación efectivamente ejecutada:** diez repeticiones consecutivas de cada caso (**20 ejecuciones correctas**) sobre la base aislada `meteocentro_phase7_test`; después, ensayo completo con las imágenes ARM64 existentes y `--backend-tests`: **184 pruebas correctas**, dos avisos conocidos de deprecación, **19,19 s** de pytest y **64,1 s** de ensayo completo. Se ejecutaron de nuevo migración/downgrade, recreación de API con IP distinta, reinicio real de PostgreSQL temporal y restauración en volumen nuevo, con huellas de observaciones/exclusiones/auditoría coincidentes. Resultado en `runtime/phase7-local-revalidado/result.json`, log en `runtime/phase7-local-revalidado.log`. La base usada solo para las repeticiones queda detenida.

**Mejora del ensayo:** `--keep-running` conserva únicamente un ensayo correcto para revisión visual; `--web-port` permite fijar su puerto loopback. El origen de autenticación coincide con su URL efectiva. Se genera un script para retirar solo esos contenedores/red y conservar los volúmenes. También se escribe `result.json` si hay un fallo. Comprobación adicional con una opción de pytest deliberadamente inválida: salida 4, informe `failed`, contenedores/red retirados incluso con `--keep-running`; evidencias en `runtime/phase7-failure-report-check/`. Ese fallo inducido verifica el manejo del error y no se cuenta como una suite correcta.

**Revisión visual disponible:** [http://127.0.0.1:8088](http://127.0.0.1:8088), cuenta `fixture`, contraseña sintética `Phase7 synthetic password only!`. Los cuatro contenedores `meteocentro-phase7-033f0dd2a4-*` quedan abiertos, con esquema `0006_operations` y datos sintéticos. Comprobado en Chrome real: inicio de sesión → Fuentes → latido «En marcha» → cierre de sesión, mediante Nginx/API/PostgreSQL reales, sin errores JavaScript. Captura en `runtime/phase7-local-revalidado/admin-preview.png`. El mapa vacío es esperado: la única estación está excluida para ensayar la conservación de esa decisión. No representa las estaciones reales. Para retirar esta vista previa: `sh runtime/phase7-local-revalidado/stop-preview.sh`.

**Instalación habitual y límites:** [http://localhost:5173](http://localhost:5173) sigue respondiendo con su API/esquema `0004_history`; todavía no incluye el backend actualizado de fases 6–7. Confirmadas únicamente las banderas no secretas del worker: `AEMET_ENABLED=true` y `METEOCLIMATIC_ENABLED=true`. Ambas redes permanecen activas allí; solo el ensayo sintético las desactiva. No se migró esa base ni se sustituyeron sus contenedores. No se ha desplegado, usado SSH, reiniciado un host ni validado systemd/Podman en servidor. Ruff, formato y `git diff --check` correctos. Instrucciones de revisión, repetición y despliegue en [OPERACION.md](OPERACION.md).

## Registro de fase 7 — 21 de septiembre de 2026

**Alcance:** el usuario pide preparar la fase 7 y sus instrucciones, **sin desplegar**. No se ha usado SSH ni inspeccionado/modificado el host. Se han leído las guías de los proyectos locales `finantialApp` y `nueva_web_julio` para conservar el flujo conocido. La aclaración del usuario mantiene **AEMET y Meteoclimatic activos para uso privado** en el ejemplo nuevo; no se ha tocado el `.env`, la base ni los contenedores que ya utiliza. WU sigue aplazado.

**Entrega:** Containerfiles de backend y web con bases fijadas por digest, usuarios sin privilegios para API/worker/web, frontend estático y proxy Nginx; cuatro Quadlet, red y volumen con nombres propios y opción rootless/rootful. Solo web publica loopback. Configuración privada separada de claves del worker, preparación sin sobrescritura, manifiestos por SHA/ID/digest, migración única con exclusión mutua, espera acotada de DB y control de fallos. CI con Actions fijadas por commit, construcción/ensayo ordinarios y publicación GHCR únicamente mediante opción manual; ningún despliegue por push.

**Operación:** [OPERACION.md](OPERACION.md) incluye inventario futuro, rutas, usuario/linger, TLS/vhost, conservación de datos locales, instalación, actualización, recuperación y diagnóstico. Dump completo con snapshot compartido y huellas de datos/exclusiones/auditoría, restore en volumen nuevo aislado, checksum, retención 7 diarias y 4 semanas, timer optativo y copia externa cifrada separada de secretos. No hay purga de históricos. `apply` no modifica Nginx/certificados y exige adopción explícita de un volumen importado.

**Diagnóstico:** migración `0006_operations`, latido persistente cada 20 s y aviso tras 90 s sin él. CLI y panel privado distinguen worker ausente, recogida retrasada y proveedor que responde con datos antiguos; muestran próxima ejecución y última incorporación. Las redes desactivadas no generan avisos por sus trabajos vencidos. No se añade mensajería ni reinicio por antigüedad meteorológica.

**Pruebas automáticas finales:** **184 pruebas correctas** de backend/despliegue sobre PostgreSQL 17.11 temporal en `127.0.0.1:55438` (20,49 s), incluidas autenticación del diagnóstico, worker sin latido, respuestas antiguas, exclusión mutua de migración, separación de secretos, rechazo de imagen mutable/dump corrupto y retención. Los HTTP meteorológicos son sintéticos. Catorce recorridos Playwright completos y repetición de cuatro de administración, uno nuevo para los avisos de operación: **15 recorridos distintos correctos**. Compilación frontend con Node 24 dentro de la imagen; Ruff, formato, sintaxis YAML de CI, compileall y `git diff --check` correctos. Se mantienen los avisos conocidos de tamaño de chunks y deprecaciones de Starlette/TestClient.

**Contenedores y esquema:** construidas las dos imágenes finales ARM64 desde los Containerfiles versionados, con `uv.lock`, `package-lock.json` y bases inmutables; API/worker UID 10001 y web UID 101. Se reutilizaron las cachés locales de esas dependencias después de cortes/timeouts de descarga; no se cambiaron versiones ni servicios existentes. La descarga de una revisión más reciente de la imagen PostgreSQL falló; se fijó el digest multi arquitectura de PostgreSQL 17.11 ya usado y verificado en fases anteriores, y lo mismo para Node 24.21.0. No se ha validado aquí una construcción AMD64 completa; la CI la prepara. Imágenes marcadas como ensayo (`dirty=true`), rechazadas por los comandos de instalación/publicación hasta construir un commit revisado limpio.

**Ensayo ejecutado dos veces, última ejecución 32,0 s:** cuatro contenedores nuevos con nombres aleatorios, red/volumen propios y límites de memoria. Migración real 0005→0006→0005→0006 con estación, origen, observación, exclusión, auditoría y cuenta sintéticas, huellas conservadas y Alembic `check` sin diferencias. Login real y estación excluida ausente; HTTP/SPA y API/CSV/gestión anónimos protegidos. API recreada con **IP distinta**, comprobando recuperación DNS del Nginx del contenedor. Reinicio real del PostgreSQL temporal y recuperación del worker; detección de worker detenido simulando el transcurso de 100 s en su latido. No se presenta esa simulación como una espera real de 100 s ni como un reinicio del host.

**Backup/restauración efectivamente ejecutados:** dump PostgreSQL completo mediante snapshot exportado; restauración en otro contenedor sin red ni puertos y volumen nuevo. `pg_restore --exit-on-error --single-transaction` correcto; coincidencia de revisión, conteos y dos huellas por tabla crítica, incluidas la observación, exclusión, auditoría y cuenta. Dump final sintético: **88.681 bytes**. Se conservaron los volúmenes sintéticos de origen y restauración; los contenedores/redes de ensayo quedaron retirados y la base temporal de tests detenida. Evidencias locales ignoradas por Git: `runtime/phase7/rehearsal-final/result.json`, `restore.json`, manifiestos y logs; reproducción con `deploy/scripts/rehearse.py`.

**Quadlet y Nginx:** generación real de los seis servicios con Podman 4.9.3, rootless y rootful, dentro de un contenedor local sin systemd activo. Se corrigió `NetworkAlias`, incompatible con 4.9.3. `nginx -t` del vhost nuevo pasó en Nginx 1.28 con certificado sintético; configuración web final Nginx 1.30 también validada con `nginx -t`, proxy/API reales y sistema de archivos de solo lectura. No acredita los vhosts/certificados ni la ejecución Podman del host.

**Recursos medidos del ensayo, sin carga de producción:** DB **23,79 MiB**, API **78,53 MiB**, worker **63,86 MiB**, web **5,836 MiB** de memoria; límites respectivos 1 GiB / 512 MiB / 768 MiB / 128 MiB. Base **9.500.339 bytes**, con una observación sintética. No se deriva una tasa de crecimiento ni capacidad de producción de este tamaño. La guía explica la medición diaria pendiente sobre el archivo real.

**Límites:** no se han ejecutado los comandos de instalación `apply` sobre systemd, cierre de SSH, reinicio de host, TLS real ni recuperación completa del servidor. No hay publicación GHCR, ejecución de CI remota, copia externa cifrada ni datos reales de consumo/crecimiento en producción. Estos pasos permanecen pendientes; los objetivos de 24 h de pérdida máxima y 2 h de recuperación no son garantías. No se han hecho nuevas consultas meteorológicas ni activado importaciones. Las comprobaciones ordinarias usan datos sintéticos.

**Siguiente paso:** cuando el usuario solicite desplegar: inventariar `ssh remote`, elegir dominio/modo/rutas/puerto, presentar los archivos concretos, revisar recursos, ensayar importación de su archivo y ejecutar la guía conservando todos los demás servicios.

## Registro de fase 6 — 21 de septiembre de 2026

**Cambios:** panel `/gestion` con estaciones, revisión, excluidas, fuentes, descubrimiento, trabajos y auditoría. Confirmación de «Eliminar de la app» con nombre/redes y motivo opcional; restauración explícita y advertencia de huecos. Pausa de un solo origen, revisión de coordenadas con evidencia, vinculación y separación conservando las series y exclusiones, y archivo privado de observaciones/resúmenes. Se mantiene Madrid como vista inicial. Documentación de uso, configuración, API y límites en [ADMINISTRACION_FASE_6.md](ADMINISTRACION_FASE_6.md).

**Acceso:** lectura privada por defecto; CLI interactiva para crear/cambiar contraseña o revocar sesiones, scrypt con sal, cookies HttpOnly/SameSite y Secure obligatoria en producción HTTPS. Sesiones opacas revocables con caducidad; autorización de backend para todas las rutas privadas, incluidos CSV y OpenAPI. Origen exacto, JSON y CSRF en escrituras. Límites de acceso persistentes por usuario, dirección y global; errores de validación sin contraseñas. Las claves siguen fuera del panel y de la API Compose. Sin registro abierto ni contraseña por defecto.

**Exclusión y trabajos:** exclusión/revocación, auditoría, versión de catálogo, cancelación de trabajos individuales y retirada de sus arrendamientos en una misma transacción. Se conserva el archivo y la identidad externa. Los lotes nacionales descartan estaciones excluidas antes de escribir; una restauración no devuelve al worker cancelado su token. Encolado histórico coordinado y comprobado de nuevo tras bloquear la estación. Vincular a una excluida mantiene la exclusión; separarse conserva una exclusión propia heredada. Las decisiones explícitas sobre duplicados persisten frente a redescubrimiento. Los CSV se generan bajo las reglas vigentes, sin archivos públicos cacheados; cabeceras no-store y versión de catálogo en las lecturas.

**Descubrimiento:** trabajos deduplicados y limitados a una solicitud manual por proveedor cada quince minutos, con las cuotas y prioridad del worker. Verificación de ID mediante proveedores/destinos permitidos, sin URLs arbitrarias. AEMET comprueba actualidad e inventario; Meteoclimatic usa el XML y deja las posiciones pendientes al catálogo ordinario. Progreso y errores seleccionados en el panel. Wunderground sigue aplazado. Ninguna acción envía bajas ni restauraciones a las redes externas.

**Pruebas efectivas:** **176 pruebas correctas** de backend, de ellas **23 nuevas de fase 6**, con PostgreSQL 17 temporal en `127.0.0.1:55437`. Incluyen autorización de todas las rutas enumeradas desde OpenAPI, sesión caducada/revocada, rotación, usuario deshabilitado, cookies de producción, CSRF/origen, límites persistentes, archivo y CSV previos a exclusión, rollback atómico, redescubrimiento, revisión y herencia al vincular/separar. Carreras con sesiones independientes y contención observada en `pg_locks`: exclusión durante descarga, escritor esperando a la exclusión y exclusión esperando al escritor; importador cancelado, restauración antes de terminar su descarga y rechazo del token antiguo. Todo el HTTP meteorológico de estas pruebas es sintético.

**Interfaz real y fixtures:** **14 recorridos Playwright** correctos con HTTP sintético, más **un recorrido separado navegador → API → PostgreSQL** con cuenta/estación sintéticas en `meteocentro_phase6_ui_test`. Login, confirmación, retirada de lista y ficha, conservación de archivo privado, restauración auditada y logout verificados. Capturas de escritorio, modal y móvil de 390 px inspeccionadas, sin desbordamiento horizontal. La prueba integrada se omite en la suite ordinaria salvo configuración explícita. Alta de cuenta por CLI ensayada en terminal real, con dos entradas sin eco, únicamente en la base temporal.

**Persistencia y esquema:** migración `0005_administration`, con límites de acceso y versión de catálogo. Alembic upgrade, downgrade a `0004_history`, nuevo upgrade y `check` correctos en la base de pruebas, sin diferencias con el modelo. Reinicio real del PostgreSQL temporal tras confirmar una exclusión: la API siguió devolviendo 404 y un worker nuevo descartó el mismo ID redescubierto con fixture, conservando una identidad y su observación previa. Solo la restauración explícita devolvió la lectura. No se afirma un ensayo de reinicio de contenedores Podman/Quadlet, que sigue en fase 7.

**Comprobaciones restantes:** Ruff, formato, compilación frontend, Compose `config --quiet` y `git diff --check` correctos. CI incluye fase 6; no se ejecutaron acciones remotas. Las dependencias y sus bloqueos no cambiaron. La consulta de versión añade una lectura constante a la proyección por lote del mapa; se actualizaron las pruebas de número de consultas y de refresco con sesión. Se mantienen los avisos conocidos de tamaño de chunks y deprecaciones.

**Límites y operación:** sin HTTP real a proveedores, sin nuevas credenciales de producción, sin despliegue y sin migración ni cuentas en la base de uso del propietario. No se modificaron los contenedores existentes; los servicios temporales de prueba se detienen al terminar. Las exclusiones, auditoría y sesiones forman parte de la copia completa de PostgreSQL; la recuperación operativa se ensayará en fase 7. La web nueva requiere la API nueva: para usar esta versión en el Compose existente, actualizar API/worker, aplicar la migración y crear el administrador según la guía. Los permisos externos de Meteoclimatic y la ausencia de diario remoto/CSV siguen vigentes. Entrega preparada en la rama `codex/fase-6-administracion`, mediante pull request contra `main` a petición del usuario. Las comprobaciones de GitHub Actions se consultan por separado en la PR.

**Siguiente paso:** fase 7 cuando se solicite: comprobar servidor, desplegar con Podman/Quadlet sin alterar otros servicios y ensayar reinicio, actualización y restauración de copias que incluyan exclusiones y auditoría. La lectura seguirá privada hasta decidir su exposición.

## Cierre local de fase 5 y vista inicial de Madrid — 21 de septiembre de 2026

**Entrega:** fase 5 completada para el alcance documentado, en la rama `codex/fase-5-historicos`, mediante la [PR #5 — Fase 5: históricos, resúmenes e importación AEMET reanudable](https://github.com/jrabalsegura/meteocentro/pull/5), abierta contra `main`. Incluye los ajustes solicitados durante la revisión local: montaje del GeoJSON en Compose, radio ampliado de duplicados de 1 km, aceptación de provincia aproximada y vista inicial de Madrid.

**Preferencia persistente de interfaz:** sin `view` válido, el mapa abre en longitud `-3.70765`, latitud `40.42437`, zoom `10.69`, coincidente con la vista elegida por el usuario. Los enlaces con vista explícita prevalecen; «Ver las cuatro provincias» conserva el encuadre regional. No se aplican filtros de provincia/red por ese encuadre. Queda recogido en `AGENTS.md`, resumen global, decisiones, especificación de fase 4 y guía de uso para mantenerlo en las fases siguientes.

**Validación final:** suite completa `pytest -q -x --tb=short`: **153 pruebas correctas** en PostgreSQL 17 temporal; once recorridos Playwright correctos con HTTP sintético y web servida desde Compose. Los dos recorridos con fixtures regionales se ajustaron al botón de las cuatro provincias; se repitieron los siete del mapa esperando al final del encuadre, con 41 marcadores y cero colisiones en el caso de 2.000 estaciones. Compilación frontend dentro del contenedor Node 24, Alembic `check`, Ruff, formato, Compose `config --quiet` y `git diff --check` correctos. Apertura real de `/` inspeccionada en navegador: Madrid y sus estaciones visibles. Se mantienen los avisos conocidos de tamaño de chunks y deprecaciones de herramientas.

**Límites y siguiente fase:** continúan las limitaciones concretas del archivo externo, la migración y las mediciones detalladas en el registro de fase 5; el cierre no implica despliegue remoto, importación masiva, purga ni disponibilidad sostenida. Web, API, PostgreSQL y worker locales quedan en marcha para el usuario; la base temporal de pruebas se detiene al terminar. Siguiente paso: fase 6 cuando se solicite. La CI remota se verifica por separado en la PR.

## Ubicaciones aproximadas junto al límite provincial — 21 de septiembre de 2026

**Cambio solicitado y aplicado:** se elimina el rechazo por incertidumbre de ±1 minuto alrededor de las coordenadas. La provincia se asigna por el punto publicado, manteniendo `precision=minute` y la procedencia; una estación próxima al límite ya no queda oculta por ese motivo. Los puntos fuera de las cuatro provincias y las exclusiones siguen tratándose por sus criterios propios. La política se aplica también al descubrimiento futuro.

**Catálogo existente:** `catalog_cli recheck-boundaries`, primero simulado y después confirmado con `--apply`, habilitó las nueve estaciones pendientes: Aranjuez, El Espinar–San Rafael, Higuera de las Dueñas, Montejo–Hayedo de Montejo, Orea, Orea 1500, Patones de Abajo–Ayto, San Rafael y Siguero. Revisión auditada e idempotente sin HTTP ni cambio de coordenadas. API local: **139 estaciones visibles**, cero revisiones `uncertain_boundary`, dos posibles duplicados y tres fuera del ámbito. En la comprobación había 118 temperaturas recientes y 21 estaciones pendientes de la próxima recogida ordinaria, programada a las 10:53 CEST.

**Validación:** **67 pruebas correctas** de fases 1 y 3 con PostgreSQL 17 temporal y fixtures; se actualiza el contrato de límites y se comprueban incorporación de observaciones, precisión expuesta en la API, repetición y exclusiones por estación, origen e identidad. Ruff, formato y `git diff --check` correctos. Imagen del worker reconstruida con dependencias bloqueadas y contenedor local actualizado; API, web y base conservadas. PostgreSQL temporal de pruebas detenido tras verificar. Sin descargas diagnósticas adicionales ni cambios de cuotas, credenciales o históricos.

**Siguiente paso:** llegada del XML ordinario para las estaciones habilitadas; quedan cinco fuentes pendientes por otros motivos. Los registros anteriores conservan los conteos y criterios vigentes en cada comprobación.

## Radio de duplicados reducido a 1 km — 21 de septiembre de 2026

**Cambio solicitado:** el radio ampliado por precisión a minutos o nombre coincidente pasa de 3 km a 1 km. Se mantiene el criterio de 250 m para coordenadas exactas sin nombre coincidente. No se fusionan estaciones ni se mezclan sus series. Se añade `catalog_cli recheck-duplicates`, explícito y sin HTTP, con simulación por defecto y confirmación mediante `--apply`, para aplicar el cambio a candidatos anteriores.

**Aplicación local:** simulación y confirmación del mismo resultado: 14 parejas conservadas como `outside_radius` con auditoría, doce estaciones liberadas, incluida Madrid–Tetuán. San Rafael permanece en revisión por límite provincial incierto. Resultado: 130 aptas, dos posibles duplicados dentro de 1 km, nueve ubicaciones de límite incierto y tres fuera del ámbito. Imagen del worker reconstruida con dependencias bloqueadas y contenedor actualizado; API, web y base permanecieron en marcha. No se han alterado exclusiones, credenciales, históricos ni cuotas.

**Pruebas efectivas:** `pytest tests/test_phase3.py -q -x --tb=short`: **53 correctas** sobre PostgreSQL 17 temporal, incluidas catorce nuevas comprobaciones del umbral (999/1.001 m y 249/251 m), exclusiones por estación/origen/identidad, fuente no disponible, traslado propuesto, otra pareja aún próxima, límite provincial e idempotencia de la revisión. Ruff, formato y `git diff --check` correctos. La API local confirma 130 estaciones y devuelve Madrid–Tetuán; 118 tienen temperatura reciente y las doce recién habilitadas esperan el siguiente XML, previsto a las 08:53 UTC / 10:53 CEST. No se adelantó la recogida ni se hizo una descarga diagnóstica adicional. La operación no borra candidatos ni reactiva fuentes excluidas. PostgreSQL temporal de pruebas detenido al terminar; worker local operativo.

**Siguiente paso:** recepción del ciclo ordinario para las estaciones liberadas. Permanecen catorce fuentes en revisión; este cambio no acredita resolverlas ni exhaustividad del catálogo.

## Comprobación del primer ciclo Meteoclimatic — 21 de septiembre de 2026

**Diagnóstico:** tras activar el worker, el primer XML descubrió 144 identidades del ámbito, aún sin coordenadas verificadas y sin observaciones guardadas. El catálogo dejó 118 fuentes aptas, 15 posibles duplicados, ocho posiciones de límite incierto y tres fuera del ámbito. Entre la resolución del catálogo y la siguiente consulta ordinaria, las 118 estaciones aparecían sin datos.

**Resultado real:** se observó el segundo ciclo programado, sin adelantarlo ni efectuar descargas diagnósticas adicionales. A las 08:22:56 UTC (10:22:56 CEST) se recogió el XML y se insertaron 118 observaciones. PostgreSQL, la API del mapa y el navegador con filtro Meteoclimatic confirmaron **118 temperaturas recientes y cero estaciones sin dato**. Se recargó la pestaña conservando filtros y posición. AEMET conservaba 1.611 observaciones; su segundo ciclo fue idempotente, sin nuevas filas. La recogida local queda en marcha conforme al arranque del usuario.

**Cambios y límites:** no se modificó el código ni la cadencia. Se documenta la espera inicial en [OPERACION_FASE_3.md](OPERACION_FASE_3.md). `git diff --check` correcto; no se repite la suite automática por un cambio de documentación. Esta comprobación acredita el arranque observado, no disponibilidad sostenida ni datos para las 26 fuentes que siguen en revisión. La interfaz todavía no explica específicamente esta fase inicial de descubrimiento.

## Corrección de arranque web en Compose — 21 de septiembre de 2026

**Problema y corrección:** el montaje de Compose solo incluía `frontend` en `/app`; el import relativo de `WeatherMap.tsx` no encontraba `config/provinces.geojson`. Las comprobaciones previas de navegador y compilación se habían realizado fuera de ese contenedor. Se conserva ahora la estructura relativa en `/app/frontend` y se monta exclusivamente el GeoJSON en `/app/config/provinces.geojson`, en modo de solo lectura. El volumen de dependencias sigue separado.

**Verificación efectiva:** `docker compose config --quiet` y `docker compose up -d --no-deps web` correctos; recreada únicamente la web. `docker compose exec -T web npm run build` correcto con Node 24.21.0; persiste el aviso conocido de tamaño de chunks. Navegador real en `localhost:5173`: interfaz y cartografía visibles sin el error de importación. `/health/ready` devuelve HTTP 200 y revisión `0004_history`; `/api/v1/stations`, HTTP 200 con cero estaciones. API y PostgreSQL mantienen sus contenedores existentes saludables. `git diff --check` correcto.

**Estado local y siguiente paso:** los tres servicios quedan en marcha para la revisión del usuario. El worker no estaba arrancado y no se activa al corregir este fallo; no se han descargado observaciones ni incorporado fixtures a la base. Para comenzar la recogida, ejecutar `docker compose --profile ingestion up -d --build`. No se han modificado credenciales, borrado volúmenes ni realizado acciones remotas.

## Registro de fase 5 — 19 de septiembre de 2026

**Cambios:** `/series`, `/daily`, `/records`, `/export.csv` por estación y consulta diaria de red; validación de origen/métrica/resolución/intervalo, paginación y límites. ECharts 6.1.0 fijado: 24/48/72 h, 7/14 días, fechas personalizadas, tabla/gráfico, zoom, cortes en huecos y extremos conservados. Diarios, meses, años y efemérides con cobertura, procedencia y cambios de emplazamiento; mapa diario de mínimos, máximos y precipitación entre grupos comparables. Selección de origen persistente, sin cambiar a otro durante el refresco. CSV solo AEMET, con límites, exclusiones y neutralización de fórmulas en texto.

**Cálculo y almacenamiento:** versión `local-v1`, días civiles Madrid de 23/24/25 horas y aritmética UTC incluso si el controlador devuelve una zona local. Medias ponderadas por intervalos/cadencia, nulos sin rellenar, dirección vectorial con calma, presión de estación y al mar separadas. Diferencias de contador con base/reinicio explícitos, descensos y huecos señalados; incrementos solapados descartados, móviles no sumados y totales que cruzan ventanas no repartidos. Cobertura por métrica, umbral configurable 90 % y corte horario común para provisionales. Correcciones marcan días y vecinos mediante trigger; el worker regenera horarios/diarios atómicamente, con revisión horaria/nocturna. Las comparaciones omiten agregados pendientes.

**Migración:** `0004_history` convierte observaciones en particiones mensuales, preservando IDs y unicidad semántica con nulos. PK/FKs incluyen `observed_at`; índices por origen/tiempo y origen/métrica/hora. Creación del mes actual y tres futuros y partición de reserva para fechas fuera de calendario. Simulación configurable a 24 meses, sin mecanismo de purga activable. La conversión requiere mantenimiento por su bloqueo y reescritura; no se aplicó a la base de desarrollo existente.

**Contrato importado:** diarios AEMET separados de observaciones y del cálculo local, con originales, hashes, revisiones, fecha de recogida y cobertura desconocida. Precipitación 07–07 UTC; `Ip` y `Acum` no se convierten en cero. En las demás métricas se conserva la fecha AEMET con límites diarios no acreditados; no se inventan ventanas Madrid ni fechas exactas para horas ambiguas. Las efemérides del proveedor se distinguen de las calculadas. El adaptador mantiene histórico intradiario `unsupported` y capacidad diaria independiente. No se reconstruyen curvas antiguas.

**Importación reanudable:** ventanas de hasta treinta días, deduplicación de solicitudes solapadas bajo bloqueo, prioridad inferior a actualidad, presupuesto propio de veinte GET/día además de cuota global/reserva, cursor confirmado con los resúmenes antes del acuse. Reinicio después del commit sin repetir descarga; reapertura explícita para correcciones, exclusión comprobada antes de descarga y escritura, y ventanas no disponibles registradas sin datos ficticios. La API pública no encola importaciones. Operación local por CLI, sin administración de fase 6.

**Pruebas efectivas:** suite completa **135 pruebas correctas**, con **24 de fase 5**, en PostgreSQL 17.11 temporal de `127.0.0.1:55436` y HTTP sintético. Casos de 23/25 horas y hora repetida, cero/nulo/hueco, contador 2,0→2,4→2,4→3,1 = 1,1 mm observado, reinicios, descensos, intervalos solapados, correcciones de lluvia a cero/nulo, ponderación irregular, picos, calma/vectores, orígenes y duraciones incompatibles, corrección/regeneración, exclusión de todas las rutas/CSV, fórmulas, presupuesto, importación/repetición/reinicio/corrección, ausencia de archivo, poblaciones con cortes distintos, particiones futuras, FK temporal y simulación no destructiva. La migración de ida/vuelta se comprobó **también con observaciones, revisiones y últimos valores existentes**, conservando IDs y métricas. Alembic `check`, Ruff, formato y compilación correctos. CI ampliada; no se ejecutó ni se afirma resultado remoto.

**Navegador:** **11 recorridos Playwright correctos**, incluidos los siete del mapa previo y cuatro de históricos: escritorio 1440 px, móvil 390 px, nulos/cortes, tabla, diarios, efemérides, errores y recuperación, exclusión, selección de origen tras refresco y retirada explícita. Capturas inspeccionadas. La primera ejecución quedó afectada por la suspensión del Mac; se reconectó y se repitió correctamente. Con API y PostgreSQL reales sobre el fixture anual, el gráfico de 365 diarios se mostró en **436 ms**; comprobados escritorio/móvil y mapa diario de veinte orígenes, cero errores JavaScript y sin desbordamiento tras completar el redimensionado. Se bloquearon teselas externas para aislar esta prueba; no es una prueba nueva del IGN ni de un teléfono físico. Se corrigió la preoptimización de Vite para no perder el worker MapLibre al actualizar dependencias.

**Volumen y consultas:** base de pruebas separada con **1.051.200 observaciones sintéticas**, veinte fuentes y un año a diez minutos. Agregados regulares sintéticos; sus consultas usan las rutas de producción, mientras el cálculo meteorológico se prueba en la suite. Medianas de cuatro lecturas calientes: serie anual de 8.760 horas **343,5 ms**, resumen mensual del año **9,75 ms**, efemérides **6,7 ms**. Respuestas: 4.797.030 / 8.569 / 1.089 bytes respectivamente. `EXPLAIN ANALYZE`: `Index Scan` sobre `ix_hourly_source_metric_time` + `Incremental Sort`, **2,819 ms**, sin lectura de detalle para construir el gráfico anual. Fila sintética de una métrica: media **322,88 bytes**; heap **358.514.688**, índices **289.021.952**, total **648.077.312 bytes**. No se extrapolan estos tamaños al archivo multimétrica ni se afirma rendimiento probado a 52,6 millones de filas. Evidencias y plan en `runtime/phase5/annual-benchmark.json`, fuera de Git; reproducción en la guía.

**Integración real separada:** dos GET preparatorios verificaron metadatos oficiales. Después, el importador y la cola descargaron `2462`, del **1 al 30 de agosto de 2026**, en **tres GET** (sobre, metadatos, datos), con techo propio de seis GET para el piloto. Base nueva `meteocentro_phase5_pilot`, sin alterar inventario ni archivo de desarrollo. Resultado: **30 fechas, 180 resúmenes por métrica, 0 observaciones intradiarias**, trabajo completado y cursor persistente. Se verificaron **420 campos originales** contra los originales conservados por el importador. Total de esta tarea: **cinco GET AEMET**; ninguna petición meteorológica a Meteoclimatic/WU. No se descargaron años ni se activó ingesta continua. Repetición, correcciones, errores y exclusión se prueban con fixtures; no se presentan como correcciones reales recibidas del proveedor.

**Limitaciones y entrega:** Meteoclimatic mantiene archivo local de observaciones y contador diario de reinicio desconocido; diario remoto y CSV de derivados permanecen pendientes. Cobertura diaria externa nula, ninguna garantía de archivo horario antiguo o de disponibilidad sostenida. Una partición de reserva ya poblada no se redistribuye automáticamente; no se borra ningún dato. ECharts/MapLibre usan chunks diferidos con aviso de tamaño; permanecen avisos de Starlette/TestClient y de Node local 26 frente al 24 fijado. Auditoría npm: cero vulnerabilidades al instalar ECharts 6.1.0. Compose `config --quiet` y `git diff --check` correctos. API, Vite y PostgreSQL temporales detenidos al terminar, conservando las evidencias locales. Claves, respuestas, bases y capturas siguen fuera de Git. Sin commit, PR, publicación, servicios remotos, automatizaciones ni purga en esta tarea.

**Siguiente paso:** fase 6 cuando se solicite. Antes de un despliegue de fase 7, aplicar la migración con mantenimiento y respaldo conforme a esa fase, revisar publicación/licencias y medir con la mezcla real de sensores. No ampliar automáticamente el piloto histórico.

## Registro de fase 4 — 19 de septiembre de 2026

**Cambios:** rutas `/`, `/estaciones`, `/estaciones/:id`; MapLibre 6.10.0 con cartografía topográfica/clara IGN, límites provinciales, números coloreados, agrupación de colisiones sin promedios meteorológicos, leyenda y controles; tabla ordenable/paginada, filtros y selección en URL, ficha por origen/variable y panel móvil. Refresco cada 60 s visible y al volver; sin llamadas meteorológicas externas desde el navegador. API `/map` por lote con bbox, provincia/red, métrica, búsqueda y frescura, y `/stations/:id/current`; elegibilidad única, una sentencia SQL por proyección y respuestas públicas `no-store`.

**Reglas:** 90 min AEMET y 45 min Meteoclimatic; observación/recogida diferenciadas. Preferencia reciente utilizable → AEMET → Meteoclimatic con procedencia explícita. Viento en km/h, presión de estación separada de presión al mar, lluvia horaria separada del contador diario de horario desconocido. Extremos solo entre lecturas recientes del mismo tipo/unidad/periodo, con cobertura; sin comparaciones parciales cuando se trunca. El municipio independiente solo se usa si consta verificado; no se inventa a partir de coordenadas o nombres.

**Pruebas efectivas:** suite completa final **111 correctas** en PostgreSQL 17 temporal, incluidas **23 pruebas de fase 4**. **7 recorridos Playwright correctos** con API/teselas sintéticas: escritorio, 390 px, URL y retorno desde ficha, exclusión tras refresco y URL directa, API/cartografía caída y recuperación, teclado, pestaña oculta, origen retirado y conservación del foco al refrescar. Exclusiones reales comprobadas en PostgreSQL, incluidas identidad, origen y commit de otra sesión. `npm run build`, Ruff, formato, `git diff --check` y Alembic `check` correctos; sin nombres de claves ni URL PostgreSQL en el bundle; MapLibre cargado aparte con aviso de tamaño de chunk. Npm sin vulnerabilidades tras fijar la versión corregida. Los avisos de Starlette/TestClient y Node local 26 frente al 24 fijado se conservan explícitos.

**Mediciones:** API con 2.000 estaciones sintéticas, mediana caliente **201,8 ms** (5 lecturas tras la primera), 2.009.894 bytes. Navegador con respuestas simuladas: **488 ms** a datos útiles, **439 ms** al cambiar variable, 41 símbolos agrupados y cero solapamientos. Navegador con API PostgreSQL real local y teselas IGN reales: **933 ms** y **466 ms** respectivamente; 39 teselas HTTP 200 de ambos fondos durante escritorio/móvil, cero errores JavaScript, sin desbordamiento de página y panel móvil de 405,1 px sobre 844. Entorno M3/24 GiB/macOS 26.6.2/Chrome 153, sin ralentización. Son muestras locales de desarrollo, no una garantía de producción ni móvil real.

**Fuente real frente a fixtures:** 24 comparaciones offline de los valores/horas de cuatro muestras AEMET archivadas del 16-09-2026, una por provincia, contra la nueva proyección. Transacción revertida, ninguna llamada nueva a AEMET/Meteoclimatic y ninguna clave nueva. No se presenta como otra ingesta en vivo. Las teselas IGN sí se verificaron con red. La población de 2.000 es sintética, identificada como tal y aislada; no cambia el inventario del proyecto.

**Limitaciones y entrega:** lluvia diaria/ráfaga diaria de horario desconocido únicamente como reportadas en ficha; racha del intervalo e intensidad aún sin datos. La cadencia de frescura no promete publicación puntual. Una pantalla abierta refleja una exclusión en el siguiente refresco visible; el servidor la aplica en cada lectura. Archivo local privado según fase 3, sin radar/pronósticos ni publicación, servicios remotos o recogida permanente. Entrega en la rama `codex/fase-4-mapa-estaciones`, mediante pull request contra `main` solicitada por el usuario. API, vista previa y PostgreSQL temporales detenidos al finalizar. Las evidencias y capturas quedan ignoradas por Git. CI ampliada para fase 4; sus resultados remotos se consultan en la pull request. Los resultados anteriores corresponden a comprobaciones locales.

**Siguiente paso:** fase 5, series y agregados con ventanas y cobertura verificadas. [Guía de uso, contrato y reproducción de mediciones](USO_Y_VALIDACION_FASE_4.md).

## Cierre local de Meteoclimatic — 16 de septiembre de 2026

**Resultado:** fase 3 completada para el alcance local privado no comercial. El [contrato vigente](integraciones/METEOCLIMATIC.md) reemplaza los bloqueos generales del diagnóstico inicial. Se utiliza el aviso legal y CC BY-NC-ND 3.0 publicados, conservando originales, procedencia y atribución; no se obtuvo un permiso individual ni se afirma autorización para comercializar o publicar derivados. La exposición externa y el archivo diario importado se revisarán en sus fases.

**Cambios adicionales:** trabajo `catalog` en el mismo worker y con la misma cuota, hasta 16 fichas por ejecución, caché de posiciones de 30 días, robots de 24 horas y reintento de fichas inexistentes/ilegibles al día siguiente. Coordenadas a minutos y altitud, identidad comprobada en el encabezado, URL/fecha/método persistidos y filtro por polígonos IGN. Se preservan posiciones manuales, revisiones explícitas, exclusiones y ubicación canónica ante traslados. La presión se normaliza como relativa al nivel del mar, apoyada en documentación primaria. Lluvia y extremos diarios tienen tipos/nombres separados y `provider_day_timezone_unknown`; no se inventan ventanas o instantes de extremos ni se suman contadores. Se conservan los sensores reportados, autor público si existe y licencia; API y pantalla incorporan atribución.

**Pruebas automáticas efectivas:** **83 pruebas de backend** sobre PostgreSQL 17 real y HTTP sintético, más **5 pruebas de fase 0: 88 correctas**. Se amplían los casos de coordenadas ausentes, identidad de ficha errónea, minutos inválidos, respuesta ambigua, caché persistente, robots denegado, ficha 404 aislada, cuota reservada para actualidad, exclusión mientras se descarga una ficha, posiciones manuales y alta manual sin posición, revisión explícita que no se reactiva, traslado y contador diario que reinicia a cero. Se mantienen las pruebas de duplicados, cuatro provincias, fronteras, cuotas concurrentes, aislamiento de red y exclusiones concurrentes de los incrementos previos. Ruff y formato, Alembic `check`, `git diff --check`, `npm run build` y Compose `config --quiet` correctos. La base nueva del piloto también pasó `upgrade head`. No cambian dependencias ni archivos de bloqueo.

**Piloto real, separado de fixtures:** proceso de worker local, base nueva `meteocentro_phase3_pilot` en PostgreSQL temporal `127.0.0.1:55435`, AEMET deshabilitado en ese piloto. Presupuesto máximo 300 GET/día, 20/minuto, concurrencia uno; reserva de 110 para XML. Primer XML: 2026-09-16T14:39:45.231971+00:00, HTTP 200, 1549 registros nacionales, 143 candidatos del ámbito inicialmente en revisión sin coordenadas. Se consultaron **143 fichas públicas y un robots**, todos HTTP 200; ningún HTML completo se incorporó al repositorio. Resultado: **131 estaciones aptas** (Madrid 85, Ávila 19, Guadalajara 19, Segovia 8) y **12 en revisión** (9 por incertidumbre de límite, 3 por posición fuera de los cuatro polígonos). No quedan fichas de esa instantánea sin procesar. Los prefijos no se usan como prueba de provincia.

**Ingesta real:** segundo XML a las 2026-09-16T14:54:48.030971+00:00, respetando al menos 15 minutos entre recogidas; 1547 registros nacionales, **131 observaciones insertadas**, 0 sin cambios, 0 revisadas. El catálogo final tiene 143 fuentes; el archivo, **131 observaciones** y 0 revisiones. Cada observación agrupa métricas de un ID y hora de estación; no se cuenta cada sensor como observación nueva. El worker consumió **146 GET**, todos HTTP 200: dos XML, 143 fichas y robots. Las lecturas de documentación y dos consultas preparatorias de robots/ficha se hicieron aparte; no se presentan como llamadas del worker. El piloto no usó ninguna clave AEMET ni hizo peticiones a esa red.

**Idempotencia y contraste real:** se reprodujo explícitamente **sin red** el segundo XML ya descargado, con su fecha de recogida conservada y usando la misma ingestión y base. Resultado: 131 idénticas, cero inserciones, revisiones o identidades nuevas; cuota HTTP intacta y siguiente vencimiento restaurado. Es una repetición local de datos reales, no otra descarga en vivo. **52 comparaciones** de valores/unidades/originales y nulos con el XML pasaron en una muestra por provincia: `ESCYL0500000005004B` (05, 2026-09-16T16:41:35+02:00), `ESCLM1900000019140A` (19, 2026-09-16T16:31:49+02:00), `ESCLM1900000019208A` (28, 2026-09-16T16:42:13+02:00), `ESCYL4000000040142B` (40, 2026-09-16T16:41:56+02:00). Se contrastaron valores actuales, presión, lluvia y extremos; se verificaron hora de estación y ausencia de límites diarios inventados. No se acredita con ello el horario de reinicio diario de cada estación.

**Reinicio y API:** el proceso del piloto se detuvo entre trabajos y se reanudó antes de la segunda descarga; permanecieron las 143 fuentes, las cuotas y el vencimiento a las 14:54:45 UTC, con exactamente dos trabajos Meteoclimatic y ningún trabajo AEMET/WU. Sobre esa base real, `/health/ready`, listado provincial, fichas y últimos valores devolvieron HTTP 200. La API mostró **131 estaciones** y siguió operativa con AEMET deshabilitado. El caso inverso (Meteoclimatic deshabilitado y AEMET disponible), errores HTTP y exclusiones se verificaron con fixtures y PostgreSQL.

**Configuración y entrega:** Meteoclimatic activado en `.env` privado con referencia veraz a licencia y ámbito local; `.env.example` exige activación explícita en otras instalaciones. Clave AEMET de Radar App conservada, sin mostrarse ni aparecer en archivos candidatos o bundle; `.env` sigue ignorado y con permisos 0600. Piloto y PostgreSQL temporal detenidos al terminar, conservando archivos de la base y evidencias locales ignoradas en `runtime/meteoclimatic-pilot/`. No se arrancaron ni modificaron servicios Compose existentes ni servicios remotos; no hay servicio de recogida permanente desplegado. Entrega de fase 3 en `codex/fase-3-meteoclimatic`, mediante pull request contra `main`. Las comprobaciones de GitHub Actions se consultan en la PR; las pruebas anteriores son resultados locales.

**Límites y siguiente paso:** las 12 ubicaciones dudosas permanecen excluidas de la lectura normal hasta revisión. El catálogo refleja IDs presentes en las instantáneas, no un censo exhaustivo de toda la red. No se garantiza disponibilidad sostenida con este piloto corto. El XML no informa el horario diario configurado: el contador/extremos se muestran como reportados, sin diarios cerrados homogéneos. El histórico anterior, agregados y gráficos pertenecen a fase 5; mapa/tabla/ficha, a fase 4. Publicación externa, servicio permanente y Podman/Quadlet siguen en fase 7. Wunderground permanece aplazado y no bloquea el cierre local de fase 3.

## Aclaración previa y clave AEMET — 16 de septiembre de 2026

**Registro anterior al cierre descrito arriba.**

**Meteoclimatic:** el usuario confirma de nuevo que la precisión a minutos es suficiente. Esa precisión ya está soportada y no se exige aumentarla. Las comprobaciones anteriores localizaron coordenadas en fichas de muestra, no un catálogo completo incorporado a la app. Queda recopilar y vincular las ubicaciones por una vía permitida; el XML de observaciones no las incluye. La incertidumbre en fronteras se revisa caso por caso. El pendiente principal de activación sigue siendo aclarar el uso para conservación, normalización y publicación. La ventana de lluvia y la referencia de presión afectan a esas métricas y no bloquean por sí solas temperatura, humedad y viento.

**Configuración solicitada:** se copió únicamente `AEMET_API_KEY` desde el `.env` de Radar App al `.env` privado de este proyecto, preservando las demás variables. Se verificó en memoria que los valores coinciden, que el destino no está seguido por Git y que tiene permisos 0600. No se mostró la clave, no se añadió a archivos versionados y no se modificó Radar App.

**Comprobación real acotada:** un primer intento no salió del sandbox (`http_transport`). Con acceso de red, se hicieron tres GET del producto actual: el sobre autenticado y los metadatos devolvieron HTTP 200 y pasaron la validación del adaptador; la descarga de observaciones terminó en `http_timeout`. Esto comprueba aceptación de la clave y metadatos, pero no un ciclo de ingestión completo. No se guardaron observaciones, no se arrancó el worker ni se hizo despliegue. La copia de la clave está completada; un nuevo piloto de ingesta sigue siendo un trabajo separado.

## Registro inicial de fase 3 — 16 de septiembre de 2026

Este apartado conserva la situación del primer diagnóstico, anterior al catálogo y piloto real descritos arriba. Sus pendientes resueltos no son el estado vigente.

**Cambios realizados:** adaptador XML Meteoclimatic con DTD/entidades prohibidos, límites de tamaño/tiempo, codificaciones comprobadas, validación de unidades y hora de estación. Se reutiliza la cola PostgreSQL y el worker de fase 2: programación y presupuestos por red, un único lote Meteoclimatic para observaciones y descubrimiento, y revisión diaria local sin descargas adicionales. Migración `0003_network_catalog`, fechas de detección, metadatos por origen, historial y propuesta de cambio de posición, parejas persistentes de posibles duplicados y exclusiones por ID externo incluso antes del primer alta. Comandos locales para alta verificada y vinculación con evidencia, sin endpoints públicos de escritura. `GET /api/v1/providers` y pantalla inicial muestran el estado de las redes. Guía y límites en [OPERACION_FASE_3.md](OPERACION_FASE_3.md).

**Contrato implementado y pendiente:** temperatura, humedad, viento actual y dirección normalizados, con nulos, ceros, originales, calidad y procedencia. `QOS=0` no invalida una estación. Extremos diarios, contador de lluvia y barómetro se conservan como campos reportados con periodo o referencia sin resolver; no se convierten en lluvia incremental, presión de estación ni al nivel del mar, y no entran en últimos valores o agregados. El RSS no aporta una capacidad necesaria sobre el XML y no se implementa. Los IDs sin posición quedan en revisión; el alta manual exige evidencia y respeta la incertidumbre a minutos y los polígonos del IGN. No se han extraído fichas, endpoints internos ni coordenadas en lote.

**Pruebas con fixtures y PostgreSQL real:** 73 pruebas de fases 1–3 correctas, más las cinco de fase 0: **78 correctas**. PostgreSQL 17 local y temporal en `127.0.0.1:55435`, base aislada `meteocentro_phase3_test`; HTTP de las pruebas ordinarias totalmente sintético. Las 29 pruebas nuevas cubren XML hostil, codificaciones incluida ISO-8859-15, unidades/coma decimal/nulo/cero/centinelas, cambio de hora, `pending_terms` sin consultas, descubrimiento repetido, resolución manual de coordenadas, cuatro provincias y frontera con precisión a minutos, exclusiones por estación/origen/identidad, reaparición con otro ID, duplicados entre redes y herencia al vincular, filas repetidas/contradictorias, 403/429/503/redirección/timeout/tamaño, cuotas concurrentes persistentes e independientes, cambio de ubicación, caída sin pérdida de catálogo, proveedor deshabilitado con API operativa y ausencia total de trabajos WU. La exclusión previa al primer alta se ensaya con sesiones concurrentes y contención observada en `pg_locks`. Tres ausencias diarias señalan revisión sin borrar datos; repetir un lote no las multiplica. Se mantienen las pruebas de concurrencia y recuperación de AEMET de fase 2.

**Comandos efectivamente ejecutados:** Alembic `upgrade head`, `downgrade 0002_aemet_worker`, vuelta a `upgrade head` y `check`, sin diferencias de esquema; `pytest` para fases 1–3; `unittest` de fase 0; Ruff y formato; `git diff --check`; `npm run build`; `docker compose --profile ingestion config --quiet`. No se añadieron dependencias ni se cambiaron los archivos de bloqueo. Persisten los dos avisos conocidos de deprecación de Starlette/TestClient. La comprobación final con TestClient devolvió HTTP 200 en readiness y una lista vacía con AEMET `disabled` y Meteoclimatic `pending_terms`; ninguna cola reclamó trabajos ni hizo HTTP externo. El PostgreSQL temporal quedó detenido al terminar. No se arrancaron servicios de desarrollo existentes ni se modificaron sus volúmenes.

**Comprobación real Meteoclimatic, separada de fixtures:** `python -m meteocentro.probe_sources --meteoclimatic --aemet` terminó a las **14:11:30 UTC del 16-9-2026** con HTTP 200: **1.539 estaciones**, las 1.539 procesables para valores actuales por el normalizador, cero errores. Prefijos observados: Madrid 90, Ávila 20, Segovia 11 y Guadalajara 22; estos conteos por prefijo no acreditan ubicación ni cobertura provincial. No había campos de coordenadas. La respuesta declaró `text/xml; charset=iso-8859-15`. El diagnóstico permitió corregir dos incompatibilidades reales: HTTP 406 con `Accept: application/xml` exclusivo y codificación ISO-8859-15 inicialmente rechazada. La negociación ahora admite también `text/xml`, y una prueba sintética conserva la distinción de caracteres de Latin-9. En esta tarea se hicieron **cinco GET externos** del endpoint XML (dos fallaron con HTTP inesperado, confirmado como 406 en el segundo; tres respondieron 200, uno limitado al comienzo para identificar codificación) y **un HEAD 200**. Además hubo un intento inicial impedido por la red del sandbox. No se guardaron respuestas brutas ni observaciones reales, no se activó programación Meteoclimatic y no se ha probado su ingesta operativa autorizada. La documentación XML, RSS y de condiciones se volvió a consultar por separado.

**AEMET real:** no había `AEMET_API_KEY` ni en el entorno ni en la configuración local leída durante esta tarea; el diagnóstico devolvió `pending_access` sin efectuar llamadas AEMET. No se copiaron claves de otros proyectos. El piloto real de fase 2, descrito abajo, sigue siendo la evidencia histórica disponible; la compatibilidad de los cambios actuales con AEMET se verificó con PostgreSQL y fixtures, **sin presentar eso como un nuevo piloto real**.

**Limitaciones y siguiente paso:** Meteoclimatic sigue `pending_terms`, deshabilitado y con presupuesto operativo efectivo cero; el presupuesto de 120 GET/día es solo la configuración futura, no una cuota del proveedor. Resolver autorización y posiciones permitidas, ventana/reinicio del contador diario y referencia de presión; luego ejecutar un piloto acotado con ingesta y exclusiones reales antes de cerrar la red. La clave AEMET se configuró después mediante petición expresa del usuario; véase la aclaración anterior. Sigue pendiente un nuevo ciclo completo de ingesta. El mapa, elección visual de origen canónico, históricos y panel corresponden a fases posteriores. Podman/Quadlet y disponibilidad sostenida siguen sin validar. Wunderground permanece aplazado, sin implementación ni requisito de cierre. Cambios locales en la rama de trabajo existente; sin commit, PR nueva ni despliegue en esta tarea.

## Registro de fase 2 — 16 de septiembre de 2026

**Cambios realizados:** cliente AEMET con resolución de sobres y URL temporales, validación de host, timeout, límite de respuesta, metadatos versionados y normalizador de periodos. Catálogo como unión del lote actual y el inventario, filtro con polígonos completos, capacidades por origen y estado `historical_only`. Ingestión por transacciones cortas, revisiones de observaciones y últimos valores monotónicos. Migración `0002_aemet_worker`, trabajos deduplicados, liderazgo, propietario y arrendamiento con latido, presupuesto persistente con reserva para actualidad, reintentos, pausa por credenciales y cursor confirmado. Un trigger y el servicio de ingestión serializan exclusiones por estación. Comandos locales `--once current`, `--once inventory`, `--status`, `--resume` y `--max-runs`. Perfil Compose `ingestion`, independiente de API y web. Guía completa en [OPERACION_FASE_2.md](OPERACION_FASE_2.md).

**Contrato implementado:** AEMET actual e inventario; `fint` UTC, temperatura/humedad/presiones instantáneas, viento medio de diez minutos y una precipitación de sesenta minutos. `prec` y `pacutp` se conservan separados en calidad y se elige uno, sin sumarlos. Se distinguen presión de estación y al nivel del mar. Una falta de dato no se convierte en cero. Las coordenadas del inventario se interpretan como sexagesimales con hemisferio. Se conservan hora de observación, recogida, ventana, hash de respuesta y de metadatos, versión y revisiones. La importación diaria AEMET no forma parte de este incremento; el método de histórico del adaptador continúa `unsupported` aunque el catálogo señale capacidad climatológica.

**Pruebas con fixtures y PostgreSQL real:** suite de fases 1 y 2 en PostgreSQL 17.11 temporal, puerto local 55434, base dedicada terminada en `_test`. Los HTTP son sintéticos y no consumen cuota externa. Se verifican duplicados, correcciones y último dato antiguo; lluvia cero/nula/alternativa, presión y UTC; caché de metadatos; 401/403, 429, timeout, respuesta grande, URL hostil y cambio de contrato; cuota concurrente, reserva para actualidad, cambio de día UTC y límite de concurrencia; reinicios sin duplicar calendario, arrendamiento vencido y rechazo del propietario anterior; caída después del commit y antes del acuse; lote parcialmente confirmado; PostgreSQL fallando antes del commit; exclusión durante descarga y exclusión que confirma mientras el escritor está bloqueado. Los dos últimos casos usan sesiones distintas y contención observada en `pg_locks`, para exclusiones de estación y de origen. La suite incluye además `Retry-After` cuando `estado: 429` llega dentro de un HTTP 200 y cambio de ubicación que exige revisión. Resultado final: **44 pruebas correctas** en las fases 1 y 2; con las cinco de fase 0, **49 correctas**.

**Comandos y comprobaciones efectivas:** `uv lock --offline` y `uv sync --locked --offline` mantuvieron las versiones fijadas; `httpx` pasó de dependencia de desarrollo a ejecución. Alembic `upgrade head`, `downgrade 0001_initial`, vuelta a `upgrade head` y `check` pasaron en la base de pruebas, sin diferencias con el modelo. Las cinco pruebas de fase 0 pasaron. Ruff, formato, `git diff --check`, `npm run build` y `docker compose --profile ingestion config --quiet` pasaron. Se construyeron las imágenes API/worker con las dependencias bloqueadas. La prueba de la imagen descubrió que el paquete instalado buscaba los polígonos junto al entorno virtual; se corrigió mediante `PROVINCES_PATH` y se comprobó dentro del contenedor, sin red, la carga de las cuatro provincias y la clasificación de Madrid. No se arrancó el Compose existente ni se modificaron sus volúmenes.

**Integración real separada:** el usuario autorizó expresamente usar en memoria la clave AEMET de Radar App para el piloto, sin copiarla a configuración ni mostrarla. Una comprobación inicial hizo seis GET (dos productos con datos y metadatos): todos HTTP 200. Observaciones: 10.479 registros; inventario: 926. Los metadatos reales del 16-9-2026 validaron las reglas. Después se arrancó el worker como proceso independiente, sin navegador, en otra base local, con techo de 20 GET y tres ciclos de observación a la frecuencia ordinaria de 15 minutos más un inventario. El primer ciclo recibió 586 registros del ámbito de 49 IDs e insertó 1.758 observaciones semánticas; el inventario añadió 10 IDs, para una unión de 59, 10 exclusivamente históricos. Sus 55 registros dentro del ámbito se aceptaron; 868 quedaron fuera y tres tenían coordenadas inválidas. El segundo ciclo repitió 537 registros del ámbito: 1.611 observaciones idénticas, cero nuevas, sin duplicados ni revisiones. AEMET seguía mostrando como máximo las 07:00 UTC. El tercer ciclo repitió también 537 registros del ámbito y 1.611 observaciones idénticas, sin insertar ni revisar. Los tres ciclos se ejecutaron aproximadamente a las 07:50, 08:05 y 08:20 UTC, separados por al menos 15 minutos. El archivo terminó con **1.758 observaciones**, **49 fuentes actuales**, **59 fuentes en el catálogo** y **dos trabajos**. El worker hizo diez GET en total (seis del primer ciclo e inventario y dos en cada ciclo posterior); con la comprobación inicial, fueron **16 GET a AEMET** en toda la tarea. El worker del piloto quedó detenido al alcanzar su límite; se paró la base temporal conservando sus archivos ignorados por Git.

**Contraste con AEMET:** seis métricas normalizadas coinciden con la respuesta descargada en una muestra por provincia: `2430Y` (Ávila, 05:00 UTC), `3013` (Guadalajara, 07:00 UTC), `3100B` (Madrid, 07:00 UTC) y `2135A` (Segovia, 06:00 UTC), todas del 16-9-2026. Las rutas locales de últimos valores devolvieron HTTP 200 para las cuatro. La API listó 59 estaciones y distinguió 10 `historical_only`. La cadencia observada del producto es horaria; los periodos de medida de diez y sesenta minutos no cambian esa resolución ni garantizan que cada estación publique cada hora.

**Reinicio real:** se detuvo y arrancó exclusivamente el PostgreSQL temporal durante el piloto. El worker registró `database_unavailable`, sin confirmar trabajos durante la caída; la base conservó 1.758 observaciones, 59 orígenes y exactamente dos trabajos. Se envió SIGTERM al proceso del piloto y se reanudó otro worker, conservando el siguiente vencimiento y completando el segundo ciclo. La base de datos de desarrollo existente y los servicios remotos permanecieron intactos. Durante el desarrollo se corrigió el inicio del día de cuota para usar UTC en lugar de la zona del servidor: el piloto comenzó con la versión anterior y, al reanudar con la corrección, su contador pasó a contar los cuatro GET posteriores. Por ello los diez GET del worker se contabilizan por las ejecuciones, no por ese contador final. La persistencia y el cambio de día de la implementación final se verificaron separadamente en los tests PostgreSQL; no se presenta el piloto como prueba de agotamiento real de cuota.

**Limitaciones:** piloto acotado, sin garantía de disponibilidad sostenida ni validación de cuota diaria del proveedor. Los dos ciclos posteriores al primero no recibieron nuevas publicaciones de AEMET: la incorporación de nuevas horas se demostró con fixtures, mientras que la repetición sin duplicados y los reinicios sí se comprobaron en el piloto real. La recuperación solo alcanza las horas todavía disponibles en el lote actual; los huecos antiguos permanecen identificados. Las revisiones conservan trazabilidad, pero los agregados e importación climatológica pertenecen a fases posteriores. No hay panel web operativo ni administración. Podman/Quadlet sigue sin probarse en este equipo. Los avisos de deprecación de Starlette/TestClient no impiden las pruebas y no se han cambiado versiones para silenciarlos. No se han versionado claves, respuestas reales, bases ni configuración privada, ni se ha realizado despliegue o activado Meteoclimatic/Wunderground.

**Siguiente paso:** fase 3, después de resolver condiciones de reutilización y coordenadas de Meteoclimatic. El arranque permanente y los servicios del servidor corresponden a fase 7. Entrega en la rama `codex/fase-2-aemet-worker`, mediante la [PR #2 — Fase 2: ingestión AEMET y worker persistente y reanudable](https://github.com/jrabalsegura/meteocentro/pull/2), abierta contra `main`. Las comprobaciones de GitHub Actions se consultan en la PR.

## Registro de fase 1 — 14 de septiembre de 2026

**Cambios realizados:** proyecto FastAPI/React con PostgreSQL, migración Alembic `0001_initial`, modelo de 15 tablas, normalización y clasificación provincial comunes, contrato extensible de adaptadores, servicio de elegibilidad para todas las rutas de lectura, puntos de entrada separados para API y worker, Compose local y CI de comprobación. La base y la pantalla arrancan vacías: no hay fixtures insertados en desarrollo ni datos meteorológicos publicados. La semántica está detallada en [CONTRATOS_FASE_1.md](CONTRATOS_FASE_1.md). Versiones elegidas: Python 3.13, Node 24.21.0, PostgreSQL 17.11 y uv 0.12.13; dependencias resueltas en `backend/uv.lock` y `frontend/package-lock.json`.

**Comandos y resultados reales:** `uv lock` y `uv sync --locked` instalaron el backend con Python 3.13. `npm ci` y `npm run build` compilaron el frontend; la compilación local usó Node 26 instalado en el equipo y mostró un aviso de versión, mientras que el servicio Compose usó Node 24.21.0. `docker compose config --quiet` y `docker compose up -d --build` terminaron correctamente. En el Compose nuevo, PostgreSQL y API quedaron saludables, la migración terminó con éxito, `GET http://127.0.0.1:5173/health/ready` devolvió `{"status":"ready","schema_revision":"0001_initial"}` y `GET /api/v1/stations` devolvió cero estaciones. OpenAPI sirvió 7 rutas. Se inspeccionó la pantalla en el navegador local, que mostró «0 estaciones publicables en la base local» y el aviso de que aún no hay datos meteorológicos.

**Pruebas:** se creó además una base PostgreSQL 17.11 temporal y vacía fuera de Compose para la migración y los tests. `pytest tests/test_phase1.py -q`: 10 correctas, con restricciones reales para identidad y observaciones duplicadas con periodos nulos/no nulos; cero, nulo, inválido, cambio horario, límite provincial, exclusión observada tras otro commit, rutas vacías y esquema incompatible. `alembic check`: sin diferencias entre modelos y migración. `python3 -m unittest discover -s tests -p test_phase0.py -v`: 5 correctas. `ruff check`, `ruff format --check` y `git diff --check`: correctos. `frontend/dist`, `.env`, dependencias locales y entorno virtual están ignorados por Git; el bundle no contiene nombres de variables de claves ni la URL de PostgreSQL. Estas pruebas no usan APIs meteorológicas externas.

**Limitaciones:** la integración AEMET real corresponde a la fase 2 y no se activó aquí; la clave del otro proyecto no se copió. Meteoclimatic continúa `pending_terms`. El usuario acepta las coordenadas públicas mostradas a minutos para la representación aproximada en el futuro mapa; se añadió una clasificación conservadora para las que se verifiquen manualmente, pero no se encontró un feed de coordenadas documentado en su mapa: ver [el contrato actualizado](integraciones/METEOCLIMATIC.md). No se automatizó la extracción de fichas ni se probó su reutilización. Podman no estaba instalado en este equipo, por lo que su invocación específica sigue sin verificar; sí se ejecutó el Compose completo con Docker. GitHub Actions ejecutó backend y frontend correctamente tanto en el push como en el PR.

**Referencia de entrega:** [PR #1 — Fases 0 y 1](https://github.com/jrabalsegura/meteocentro/pull/1), fusionado en `main`; estado comprobado al preparar la PR de fase 2.

**Siguiente paso:** validar Podman cuando haya un entorno de prueba disponible; después, fase 2 para un worker AEMET separado con límites de cuota, recuperación y pruebas reales acotadas. La fase 3 de Meteoclimatic depende de aclarar licencia, método autorizado de coordenadas y significado de sus contadores.

## Registro de fase 0 — 14 de septiembre de 2026

**Cambios realizados:** [matriz por producto](integraciones/MATRIZ_ACCESO.md), [contrato AEMET](integraciones/AEMET.md), [contrato Meteoclimatic](integraciones/METEOCLIMATIC.md), diagnósticos sin registro de secretos, polígonos IGN completos y de visualización con [procedencia](../config/provinces.provenance.json), fixtures sintéticos y pruebas. Wunderground permanece `deferred_cost`.

**Comandos y resultados reales:** `python3 scripts/fetch_provinces.py` descargó una geometría oficial para cada provincia y generó cuatro polígonos completos y una versión visual simplificada; requiere red y solo debe repetirse si se decide actualizar la versión. El diagnóstico `python3 scripts/diagnose_sources.py --aemet --meteoclimatic`, con `AEMET_API_KEY` en el entorno del proceso, obtuvo HTTP 200 para observaciones, inventario y XML. `--aemet-history` obtuvo HTTP 200 para un día de la estación `2462`; sin clave, `--aemet` devuelve `pending_access` sin llamar a la red. Las respuestas AEMET se descargaron mediante las URL temporales de `datos` y `metadatos`, sin guardarlas. Dos consultas `GetCapabilities` del IGN respondieron HTTP 200. `python3 -m unittest discover -s tests -v`: 5 pruebas, todas correctas. `python3 -m compileall -q scripts tests` y `ruff check scripts tests`: correctos. El entorno inicial no permitía DNS; las consultas oficiales se ejecutaron con la autorización de red correspondiente. No se probó Podman ni PostgreSQL porque pertenecen a fases posteriores.

**Fuentes reales y capacidades:** AEMET observación actual, inventario y un día climatológico: `verified` técnico. En la instantánea final de observación había 10.039 filas, 855 IDs únicos en España y 49 IDs únicos clasificados dentro de las cuatro provincias; en inventario, 926 IDs y 55 clasificados dentro. Meteoclimatic XML nacional: 1.540 estaciones, `verified` técnico; los prefijos de los cuatro ámbitos produjeron 89/22/10/22 estaciones respectivamente, todavía sin coordenadas en el XML. Estos conteos no son cobertura garantizada ni datos almacenados. RSS Meteoclimatic e importación diaria no se probaron. Ninguna fuente está integrada en la app.

**Pruebas con fixtures:** los ejemplos de AEMET y Meteoclimatic son [sintéticos](../tests/fixtures/SINTETICOS.md). Las pruebas distinguen 0 de nulo, ISO-8859-1, coordenadas sexagesimales, clasificación geográfica, límite compartido y hora de estación frente a hora del feed. No sustituyen pruebas de ingestión ni de base de datos.

**Limitaciones y decisiones:** Meteoclimatic enlaza CC BY-NC-ND 3.0; se marca `pending_terms` antes de conservar, transformar o publicar datos. La pertenencia provincial de sus IDs por prefijo es provisional hasta disponer de coordenadas verificadas. En AEMET, el inventario no equivale a emisión actual; se observaron tres coordenadas de inventario no interpretables. Los históricos horarios antiguos no están acreditados. La clave AEMET no se copió a este repositorio ni se publicó; su JWT indica expiración el 31-10-2026 11:22:47 UTC, sujeta a validez efectiva del proveedor. `GetCapabilities` de los dos fondos WMTS candidatos del IGN respondió HTTP 200, aún sin integración visual ni prueba de teselas. No se solicitaron gastos, publicación ni acciones remotas.

**Siguiente paso:** fase 1 con base reproducible y fixtures. Antes de activar Meteoclimatic en la fase 3, resolver por una vía autorizada sus condiciones concretas y las coordenadas. Planificar renovación de la clave AEMET antes de que caduque. La fase 0 queda documentada con esos bloqueos, no como aprobación general de ambas fuentes.

## Comprobaciones de planificación previas

- Revisión de páginas públicas y presentación del mapa de Suremet.
- Consulta de documentación primaria de AEMET, Meteoclimatic, The Weather Company, Podman y componentes propuestos.
- Identificación del límite de la búsqueda de proximidad WU y de diferencias entre históricos diarios e intradiarios.
- Redacción de documentos por fase con entregables, validación y prompts para Codex.

Las comprobaciones de planificación del 9 de septiembre por sí solas no acreditaban acceso; los resultados de acceso real están diferenciados arriba. Las frecuencias y políticas de retención siguen siendo propuestas que deberán medirse.

## Registro para completar en cada fase

```text
Fase y fecha:
Cambios realizados:
Comandos o recorridos verificados:
Resultados observados:
Fuentes reales probadas y capacidades:
Pruebas con fixtures:
Limitaciones o bloqueos:
Decisiones tomadas:
Commit o referencia de entrega:
Siguiente paso:
```
